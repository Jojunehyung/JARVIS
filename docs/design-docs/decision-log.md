# Decision log
<!-- src: SPEC-0 --><!-- src: SPEC-13 -->

Dated product and engineering decisions, newest last. Rule numbers refer to [core-beliefs.md](core-beliefs.md). Korean originals of the pre-2026-09-08 documents are preserved at git tag `ko-docs-final`.

| Date | Decision | Ref |
|---|---|---|
| (undated, pre-v3) | Game effects (XP, levels, gold, criticals, bosses, story, dating-sim) abandoned completely, no backup; reintroduction would be a redesign. | Rule 7 |
| 2026-08 | Job-fit matrix evidence fixed: HRD Korea posting-usage statistics, MOEL 500-company survey, per-job hiring specs and recruiter surveys; evidence strength per cell (strong/medium/weak). | Rule 15 |
| 2026-08-29 | No AI: `askClaude`, goal coach, difficulty judgement, promotion quiz, study oral check removed; reintroduction requires explicit user approval. Custom certification D is manual (20–100); study uses a self-report mode. | Rule 7 |
| 2026-08-29 | Job-fit tier C (irrelevant) pays ×0 (research draft: 0.25). 개발/데이터·AI rows: 사무·회계 and 전문직 demoted to C; exceptions 변리사, 사회조사분석사 2급 = B. | Rule 15 |
| 2026-08-29 | Study verification tiers `STUDY_REQ` fixed: E 30 chars / D + 1 artifact / C 60 chars + 1 / B 100 chars + critique + 2. | Rule 16 |
| 2026-08-30 | Goal-first structure: tasks only from goals, `goalId` mandatory, area inherited, one-day size (general/activity ≤ C, study E/D), milestones exempt, legacy tasks in 미분류 can only be completed or deleted. | Rule 18 |
| 2026-08-30 | KR–task bridge: certification/exam milestones only by one-click from the KR; free browse mode removed; modes [일일 실행 · 학습]; `goalKinds` scoping; manual cert "done" button removed. | Rule 19 |
| 2026-08-30…09-01 | Schema v12: `metrics.risk` → `metrics.body` (appearance), reset to 15; body is self-assessment only. | Rules 8, 12 |
| 2026-09-01 | Master specification written by reverse-engineering the 3,338-line code and three docs. | — |
| 2026-09-02 | Design handoff applied: `PortraitSprite` v3, `TrophySvg` v2, five empty-state illustrations, per-screen style deltas; onboarding title copy chosen: "지금 위치를 숫자로 확인하세요. 평가는 시장 기준입니다." (game copy dropped). | DESIGN |
| 2026-09-03 | Vite scaffold + `store` shimmed to `localStorage` (call sites unchanged); single-file demo build. | [completed plan](../exec-plans/completed/2026-09-03-storage-shim-and-vite.md) |
| 2026-09-04 | CERTS V1.2: 30 health/medical qualifications added; 보건·의료 row S for its own category only; `certByTitle` longest-name matching; 전문의 not listed (D cap). | Rule 6, 15 |
| 2026-09-06 | CERTS V1.3: all 807 currently issued national qualifications added (1,011 total; three-lens estimation + adversarial verification; existing D unchanged). Categories 15, jobs 21, directions 18. The 150 new cross cells verified against mention rates → all C; 47 evidenced exceptions in `CERT_W_EXC`. Four renames applied; 2027/2028 renames deferred. Catalog cap 40 → 60. | [completed plan](../exec-plans/completed/2026-09-06-certs-v1.3.md) |
| 2026-09-07 | Dead-code cleanup, game residue removed (trophy kind boss → ach, schema v13), lookup indexes, onboarding list capped at 60 rows (DOM 4,121 → 318); useDeferredValue removed after measurement; E2E harness (`tools/e2e`) introduced and caught two regressions. | [completed plan](../exec-plans/completed/2026-09-07-rename-and-cleanup.md) |
| 2026-09-07 | Terminology rename: 실행 (task) · 영역 (area) · 인생 관리 · 보호권; identifiers, component names, file name (`LifeManager.jsx`), and docs updated; schema v14 (`quests→tasks`, `parts→areas`, `partId→areaId`, `v` normalisation); storage keys `liferpg-*` kept. | Rule 12 |
| 2026-09-07 | Evidence viewer (`EvidenceViewModal`), `ToastHost`, favicon/manifest icon; image keys cleaned on task delete and reset. | Rule 16 |
| 2026-09-08 | Harness-engineering layout: `AGENTS.md` routing, seven subagents, hooks (prompt-first, data-guard, lang-check, finish-check), `tools/harness`, English documentation; Korean originals retired after tag `ko-docs-final`. Prompt-first protocol and mandatory cleanup pass adopted. | [active plan](../exec-plans/completed/2026-09-08-harness-restructure.md) |
| 2026-09-08 | Code and E2E comments, step names and log strings translated to English (UI copy, data tables, selectors untouched; app code proven byte-identical outside comments). Language gate enabled in `finish.config.json`; `lang-check` allows Korean data vocabulary (cert/exam/job/category names from the tables) inside comments. | AGENTS.md §6 |
| 2026-09-08 | Master spec decomposed into `docs/product-specs/*` and `docs/design-docs/*`; chapters 8 and 10–13 folded into DESIGN, FRONTEND, QUALITY_SCORE, backlog and the tech-debt tracker. The four Korean source documents (product plan, kickoff procedure, master spec, design prompt) were deleted; they remain readable at git tag `ko-docs-final`. | [completed plan](../exec-plans/completed/2026-09-08-harness-restructure.md) |
| 2026-09-08 | English-first rule made explicit and enforceable: a task request produces its plan, prompt, summary and edits in English whatever language the request used; Korean stays only where AGENTS.md §6 requires it, quoted in backticks. `check-docs` now fails on Korean prose under `docs/exec-plans/`. | [completed plan](../exec-plans/completed/2026-09-08-english-plan-language.md) |
| 2026-09-09 | Daily assistant: schema v15 (`journal[]`, `reviews[]`, `tasks[].due`, `act.lastCheckin`/`briefingSeen`/`lastReview`); the briefing is derived at render (`buildBriefing`); home agenda ordered by due date; journal and weekly review; the copy/paste assistant bridge approved as the single AI touchpoint — Rule 7 amended. The assistant proposes plain tasks only and never completes, promotes, or scores. | Rules 7, 9, 12, 13, 18, 19 · [completed plan](../exec-plans/completed/2026-09-08-daily-assistant.md) |
| 2026-09-09 | Installable offline app: relative build base, PNG and maskable icons generated from the SVG, manifest with relative scope, a generated service worker (hashed assets cache-first, document network-first, old caches evicted on activate), persistent-storage request, and a backup file that carries the save with its evidence photos. Import runs through `migrate` and confirms before replacing. | Rule 12 · [completed plan](../exec-plans/completed/2026-09-09-installable-pwa.md) |
| 2026-09-11 | Schedule tab: schema v16 (`events[]`), a fifth tab `일정` with the day groups `지난 마감` / `오늘` / `내일` / `이번 주` / `이후` and per-occurrence actions (`완료 표시`, `이번 회차 취소`), repeat rules expanded at render (`occurrencesOf` / `eventsOn` / `upcomingEvents`), and the same facts repeated on the daily briefing (`오늘 일정`), the home briefing card and the assistant packet (`## 다가오는 일정 (14일)`). An event is a record, never a 실행 (task): no goal, no points, no trophy, no metric, no streak, no evidence gate, and a pasted assistant reply cannot create one. The `이후` group lists one row per event (its earliest occurrence) so a repeat cannot bury a dated deadline. | Rules 9, 12, 13 · [active plan](../exec-plans/completed/2026-09-11-schedule-tab.md) |
| 2026-09-11 | Schedule calendar view: schema v17 (`ui.scheduleView`), a `목록` / `달력` toggle in the 일정 tab and `ScheduleCalendar` — a seven-column month grid with one marker per occurrence (three dots, then `+{n}` for the rest), today and the selected day marked separately, a `선택한 날짜` panel that renders the extracted `EventRow` the day groups use, and `일정 추가` prefilled with the selected day. The month is expanded by a single `upcomingEvents` call reduced into a render-local map, so grid and panel cannot disagree and no occurrence list, month index or selection is stored — only the chosen view. Paging is bounded to ±24 months (`CAL_RANGE_MONTHS`) and selects the 1st of the month it moves to; `오늘` returns to today. | Rules 9, 12, 13 · [completed plan](../exec-plans/completed/2026-09-11-schedule-calendar-view.md) |
| 2026-09-11 | South Korean public holidays in the month calendar: a display-only `HOLIDAYS` map (2026-2027, 46 dates) and a derived `HOLIDAY_YEARS`, read only by `ScheduleCalendar`. Exactly one tone per day number — today amber, public holiday or Sunday rose, Saturday sky, everything else zinc — and `공휴일 · {name}` under the panel's date line; a month outside the covered years marks nothing and states `공휴일은 {from}~{to}년만 표시해요.` under the grid. Rows are copied from the Gwanbo gazette notices issued under the Presidential Decree on the holidays of government offices, cross-checked against the KASI almanac; the Decree amended on 2026-04-30 added `노동절` and restored `제헌절`, both substitute-eligible. Nothing here is computable, so the next year is appended only from a cited announcement and an undesignated temporary holiday is never added. A holiday is never an event: no record, no count, no briefing line, no packet line, no schema change. [Rule 6](core-beliefs.md#rule-6) applies in spirit only, so `HOLIDAYS` is deliberately not registered in `DATA_TABLES`. | Rules 6 (in spirit), 9, 13 · [completed plan](../exec-plans/completed/2026-09-11-korean-holidays.md) |
| 2026-09-11 | Goals hold certifications, study and fitness only: the `meet` activity kind and free-form kind-less normal tasks are both removed from creation under a goal (a plain task such as `이력서 초안 작성` can no longer be created; appointment-shaped work belongs to the `일정` tab). Saved `meet` tasks are converted, not deleted — schema v18 drops `kind` and keeps title, difficulty, points, completion dates and the stored minutes `evidence`. One carve-out: a task created through the count-KR bridge (`채우기 ›`) stays creatable without a kind, tracked by component state (`AddTaskModal`'s `krId`) only, never persisted, since a count KR is the goal's own measured action. Rules 17 and 19 amended. | Rules 7, 9, 12, 13, 17, 18, 19 · [completed plan](../exec-plans/completed/2026-09-11-goal-task-kinds.md) |
| 2026-09-11 | Three independent decisions landed together. Goal deletion: an active goal shows `목표 삭제` (rose), confirms by naming the goal and both counts, deletes the goal together with tasks under it that carry no completion record, and keeps completed tasks — they surface in `미분류` afterwards; the achieved-goal path `기록에서 제거` is unchanged. Tab order: `홈 · 목표 · 실행 · 일정 · 성장` (schedule moves ahead of growth) — decide, do, when, look back; the three daily tabs sit together and the record tab goes last. Life metrics removed entirely, including the stored numbers: the global self-assessed triple `metrics.asset / infl / body`, its check-in, its automatic gain from achievements/promotion/goal completion, the briefing check-in line and the packet's metrics line are all gone (schema v19 drops `metrics` and `act.lastCheckin`); what the triple claimed to measure belongs to a goal's metric KR, unaffected. Rule 8 rewritten; Rule 17's fitness-measurement clause reworded to drop the appearance-metric reference. | Rules 8, 9, 12, 13, 16, 17, 18 · [completed plan](../exec-plans/completed/2026-09-11-goal-delete-nav-metrics.md) |
| 2026-09-13 | Business tab: schema v20 (`folio[]` portfolio pieces, `rates[]` unit prices, `deals[]` period contracts, `ui.bizView`), a sixth tab `사업` before `성장` (tab order `홈 · 목표 · 실행 · 일정 · 사업 · 성장`, `grid-cols-6`) with three views `계약` / `단가` / `포트폴리오`, three modals (`modal.type` 15 → 18), and one `bizSummary(state, today)` read by the tab header, the daily briefing, the home card and the assistant packet so the four surfaces cannot disagree. A contract is a billing rule (`startMonth`, `months`, `monthly`, optional `costMonthly`) plus the user's own `paidMonths` stamps — a record, never a `실행`: no P, no trophy, no goal, no streak, no evidence gate. `cost` is never defaulted to 0; a missing cost withholds the margin line but never the price or the total, and a negative margin prints in rose rather than being hidden. A new `resizeImageFit` (aspect-preserving, never upscales) and storage guards (`persisted`, `storageUsedBytes`, `saveImageChecked`, budget check before write, verified-write readback) back the new `liferpg-img-folio-{id}` key; every image failure still saves the record and toasts why. `TAB_ACTIONS` replaces `closeBriefing`'s hardcoded three-tab chain with a whitelist of four. E2E grew 116 → 135 steps (`tools/e2e/flow8.js`, including a step proving the briefing's revenue line survives a section full of alerts); `flow7.js`'s shared `rows` / `clickExact` helpers moved to `tools/e2e/run.js`, and its weekend-tone step was fixed to count the whole grid before excluding `today` from the tone assertions (a date-dependent bug from 2026-09-11). No rule is amended: a business record is simply not a task, exactly as an event is not. | Rules 1, 9, 12, 13, 18 · [completed plan](../exec-plans/completed/2026-09-12-business-tab.md) |
| 2026-09-13 | Unified to-do list: `실행` stopped mirroring the goal hierarchy and became one time-ordered list of everything actionable — tasks, schedule occurrences, and two dated business facts — built by a new pure helper `todoOf(state, today)` that calls `agendaOf` and `upcomingEvents` rather than re-implementing either ([Rule 9](core-beliefs.md#rule-9)). Groups `기한 지남` / `오늘` / `내일` / `이번 주` / `이후` plus `오늘 완료`, and a `할 일` / `완료` view toggle held in component state, never `state.ui`. Only a task row reaches `onComplete`: an event row keeps its own `완료 표시` / `이번 회차 취소` / `수정` (paying nothing, moving no goal — [Rules 1, 10](core-beliefs.md#rule-1)), and a new `BizTodoRow` is a full-width button with no control at all. Business facts admitted are exactly two — an unpaid billed month and a contract ending inside `DEAL_END_SOON`, both capped/keyed by the already-shared `BIZ_ALERT_MAX` (renamed off the `buildBriefing` literal `slice(0, 3)`) — a stale quote is deliberately excluded: it has an age, not a date, and inventing one would be a false number under [Rule 13](core-beliefs.md#rule-13). `목표` is unchanged and stays numbers-only; removing `＋ 이 목표에 실행` leaves task creation reachable only from `목표`, a deliberate strengthening of [Rule 19](core-beliefs.md#rule-19). The home card's `오늘 할 일` narrows to overdue + today (tasks only) by reading the same `todoOf`, so the two surfaces cannot disagree about "today"; its empty copy is now `오늘 기한인 실행이 없어요 — 이번 주 이후는 실행 탭에 있어요.` **Rule 18 reading** (no rule text edited): the rule's wording, "milestones… shown under their goal in the task tab," predates this rewrite; there is no per-goal section any more, but every row — milestone or not — now states its own goal on itself (`🎯 {goal.title}` or `목표 기여 없음`), which is what makes a goal-scoped section unneeded. The rule's substance (the one-day exemption, the `Lock` control, evidence-only completion) is untouched; a wording update, if wanted, is a separate core-beliefs edit needing its own approval. One defect found and fixed during implementation: the `완료` archive previously reused the open row unchanged, so a `daily` task completed on an earlier day drew a live, unchecked checkbox that could complete it again for today from inside a list titled `완료`; a row now takes an `archived` flag that forces it closed and states `완료 {date}` (the trailing `X` stays live). [TD-02](../exec-plans/tech-debt-tracker.md) is resolved as a side effect: tasks of a **done** goal, previously invisible to both the per-active-goal loop and the orphan section, now render like any other task, since the list filters on task state only. E2E grew 135 → 139 steps. | Rules 1, 9, 10, 13, 16, 17, 18, 19 · [completed plan](../exec-plans/completed/2026-09-13-unified-todo.md) |
| 2026-09-13 | Service-worker updates apply on the next open, never mid-session: `src/main.jsx` no longer reloads the page on `controllerchange` (`hadController` / `reloading` removed); `skipWaiting()` and `clients.claim()` stay, because this app ships one bundle with no lazy chunks and no runtime asset fetches, so a page already claimed by a new worker never asks for anything the old cache's eviction removed on activate. Reported 2026-09-13: a tab switch dropped the user back to the home screen a few seconds in, once per deploy, because every build stamps a fresh `BUILD` id and the new worker claims the open page as soon as it activates. `tools/e2e/flow6.js` gained a step that registers a second `sw.js?e2e-update=1` at the same scope, in a throwaway browser context, to simulate a takeover on a page that is already controlled at load time — the exact condition the old code reloaded on; confirmed to fail on the pre-fix code (one navigation) and pass after. E2E grew 139 → 140 steps. | [completed plan](../exec-plans/completed/2026-09-13-growth-simplify.md) |
| 2026-09-13 | `성장` tab simplified around one number: `롤모델 근접도` (role-model proximity) now leads the tab as a headline (`{rg.match}%` at `text-4xl`, or `롤모델 미설정 — 근접도 계산 대상 없음` with no usable role); `roleGap` and its squared-curve formula are untouched, and the bar-legend paragraph moved into `RoleAdviceModal` instead of sitting under the bars ([Rule 14](core-beliefs.md#rule-14)). The four area cards collapsed into one-line rows under `실력 트랙 — 영역별 승급 관문`; a row with a next rank **is** the button into `PromoteModal` (which now also states `필요 증거: {next.req}`), a row at grade 9 is a plain, unclickable div, and the gate itself stays the only route to a promotion ([Rule 11](core-beliefs.md#rule-11)). `성취의 벽` and the backup/reset controls (`데이터 — 백업 · 초기화`) both collapse behind a header that states its own numbers while closed — the wall's is `트로피 {n}개 · 시험 {n}개 · 검증된 성취 {n}건` — so collapsing never removes a number from the screen ([Rule 13](core-beliefs.md#rule-13)). The two collapse flags are component `useState`; no schema change, no `state.ui` key ([Rule 9](core-beliefs.md#rule-9)). Measured `<main>` at 390 px on the demo save, everything collapsed: 1,963 px → 800 px. `tools/e2e/run.js` gained a shared `openAreaGate(name)` helper used by `flow.js`, `flow2.js` and `flow3.js`; `flow2.js` gained two steps proving the headline leads with a numerically-checked percentage and that the achievement wall states its counts while collapsed. E2E grew 140 → 142 steps. [TD-11](../exec-plans/tech-debt-tracker.md) (no way to clear a role model) is unchanged: the `rg === null` branch now also covers a role whose every area is excluded, and the button still reads `롤모델 수정`. One pre-existing E2E defect was found, not fixed, and tracked as [TD-37](../exec-plans/tech-debt-tracker.md): `flow.js`'s onboarding step 3 deselects `기본지식` and `커리어` instead of selecting them, so the main E2E save carries exactly one area, `사업`. | Rules 9, 10, 11, 13, 14, 16 · [completed plan](../exec-plans/completed/2026-09-13-growth-simplify.md) |
| 2026-09-13 | Exact CV profile: schema v21 replaces the seven-band `연령대` chip, the six-key `최종 학력` chip and the `직무 경력` chip with real facts — `profile.name`, an exact `birth` date, optional `email`/`phone`, and two new record arrays, `edus[]` (school, department, degree, dates) and `careers[]` (company, role, employment kind, dates). `eduKeyOf` / `careerKeyOf` derive the unchanged `EDU_OPTS` / `CAREER_OPTS` starting-grade keys from those records exactly once, inside `Onboarding.start()`; a CV edited later through the new `ProfileModal` writes only `profile.edus` / `profile.careers` and the other CV facts, never `edu` / `career` or any `areas[].grade` — proven by an E2E step that is mutation-tested (planting a `profile.edu` rewrite plus a grade bump in `saveProfile` fails it) ([Rule 4](core-beliefs.md#rule-4), [Rule 11](core-beliefs.md#rule-11)). `ProfileModal`, opened by a `프로필` button that replaces the home card's `업로드` button and its `✕` photo-clear overlay, is the app's first way to change anything about the user without `데이터 초기화`; it reuses the onboarding CV components (`CvSection`/`CvAddForm`/`CvEntryRow`) and declares no file input of its own, driving the root's existing `askUpload("profile")` instead. The assistant packet gains one `## 이력` line — degree + status, department or field, total months of practice, and the latest role, built from `topEdu`/`latestCareer` — that deliberately excludes the name, birth date, e-mail, phone, school name and employer name; a second E2E step confirms the exclusion, mutation-tested by prefixing the school name onto the packet's education line, which fails it as expected. `AGE_OPTS` stays frozen, referenced only by `ageText`'s legacy-band guard; `EDU_OPTS` / `CAREER_OPTS` / `MAJOR_FIELDS` / `LEAD_OPTS` / `BIZ_OPTS` / `OUTPUT_OPTS` are all unchanged. This closes the `profile`-shape half of [TD-28](../exec-plans/tech-debt-tracker.md) (the `@schema` block now lists every field `profile` carries, and `demoState.edu` is the valid key `"ba"` instead of `"univ4"`); the `roleModel`-unused, proximity-figure-wording and `CERTS`-header-comment halves remain open. The no-rescore E2E step currently compares only the `사업` area's grade because [TD-37](../exec-plans/tech-debt-tracker.md), found above, narrows the onboarding save to one selected area; it widens on its own once that is fixed. E2E grew 142 → 149 steps (`tools/e2e/flow.js`, `flow3.js`, `flow4.js`, `flow6.js`, `flow8.js`, `run.js`, and the shared bench helpers in `bench-lib.js`/`ab_onboard.js`/`rows.js`). | Rules 4, 6, 7, 9, 11, 12, 13, 16 · [completed plan](../exec-plans/completed/2026-09-13-cv-profile.md) |
| 2026-09-14 | Calendar export: the user asked to be alarmed daily about goals and schedule; a fixed-time notification while the app is closed needs either a push server (breaks the local-only design) or a local-scheduling browser API that does not exist in shipping browsers (Notification Triggers abandoned; Periodic Background Sync is browser-timed, skips days, and a service worker cannot read `localStorage`), so the app still sends none of its own. Instead it exports `life-manager-calendar-{date}.ics`, a pure `buildIcs(state, today, opts)` snapshot the phone's own calendar imports once and then raises alarms from — the user's own decision to include item titles, knowing a synced account (e.g. Google) stores them on that provider's servers. `CalendarExportModal`, opened by `캘린더로 내보내기` on the `일정` tab's chip row in both views, asks the daily reminder time and a range (`30일`/`90일`/`1년`, default 90 = `EVENT_HORIZON_DAYS`); both are component state, not stored — no schema change, schema stays v21, `modal.type` 19 → 20. Every timed appointment and open task gets its own `VEVENT` with a `VALARM`; the dozen daily tasks become **one** repeating digest rather than a dozen simultaneous alarms; goal deadlines are exported with no progress or pace number, since a number frozen at export is false the next day (Rule 9). Times are floating local time, no `TZID`/`VTIMEZONE`, matching every other date in the app; the one UTC value is `DTSTAMP`. A monthly repeat on day 29–31 is written as one `VEVENT` per date `occurrencesOf` itself returns, never `RRULE:FREQ=MONTHLY`, because RFC 5545 requires a conforming calendar to drop the months the app's clamp lands in. Excluded from the file, always: the profile/CV, any business record or amount, an event's `place`/`note`, journal, reviews, evidence, grades, progress — proven by a smoke check that builds from a state whose excluded fields throw when read, and an E2E step that plants all of them and asserts none reaches the file. `downloadBlob` is now the one shared download path for both `exportBackup` and the new `exportCalendar`. E2E grew 149 → 157 (`tools/e2e/flow9.js`, 8 steps, plus `captureDownload` in `run.js` reused by `flow6.js`'s backup step). Two things cannot be verified by automation and are recorded as pending in `install-and-backup.md` and backlog item 2: whether a given Android calendar app honours the file's `VALARM`s, and whether re-import replaces, skips or duplicates an entry. | Rules 7, 9, 12, 13, 18, 19 · [completed plan](../exec-plans/completed/2026-09-14-calendar-export.md) |
| 2026-09-15 | Home becomes one quantitative CV; the `성장` tab is removed (six tabs → five: `홈 · 목표 · 실행 · 일정 · 사업`). The user asked, verbatim: `성장탭을 없애주고, 홈화면에 정량적 평가만 남겨줘` / `복잡한거 다 지우자` / `홈화면을 한개의 CV로 만들고, 밑에 달성하고자 하는것에 대한 %만 조그맣게 따로 만들어줘`, then made four decisions when asked: (1) the figure under the CV is role-model proximity only — goal progress and pace stay in `목표`, which becomes the only screen that shows either; (2) promotion moves onto the CV's grade rows, opening `PromoteModal` exactly as the growth rows did; (3) role model, backup and reset sit behind a small `설정` button in the CV's corner (new `SettingsModal`); (4) the `오늘 브리핑` card and `오늘 할 일` leave home, the briefing still auto-opens once a day, and the journal and weekly review stay reachable from it. `HomeTab` now renders exactly two top-level children — the CV `<section>` (identity + `프로필`/`설정` buttons; records `학력`/`경력`/`자격`/`시험`/`포트폴리오`/`성취`, the last opening a new read-only `AchievementWallModal`; `영역 등급` rows, `AreaGradeRow`, moved verbatim from the growth tab) and the proximity line (`롤모델 근접도 {match}% · {name} ›` → `RoleAdviceModal`, or the inert `롤모델 미설정 — 근접도 계산 대상 없음` without a usable role). Two new helpers, placed after `displayName` and read nowhere but render: `cvSummaryOf(profile, today)`, lifted out of `buildAssistantPacket` so the packet's `## 이력` line and the CV's `학력`/`경력` rows cannot state a different degree or role (packet text proven byte-identical); `heldCertsOf(state)`, merging `profile.certs` (declared at onboarding) with done `isCert` tasks (proven with a photo) — the first time a certification earned in-app, not just declared at onboarding, is counted as held, read by both the CV and `ProfileModal`'s `보유 기록` so the two cannot disagree ([TD-42](../exec-plans/tech-debt-tracker.md): no marker distinguishes declared from earned). Not the user's own request, found necessary during implementation: with the home card gone, its `브리핑 열기 ›` was the only manual way back into the briefing once the daily auto-open is dismissed, and through it the only way to the journal, the weekly review and the assistant bridge — so it moved onto the `실행` tab's header chip row instead of disappearing; `buildBriefing` dropped its `counts` return (the removed card was its only reader) and `TAB_ACTIONS` became `["home", "goals", "schedule", "biz"]` (`"growth"` retired), with the briefing's stagnant-area lines rerouted to `home` and the no-role `다음 단계` line still opening `RoleModelModal` directly. `doneTodayCount` was deleted with its only call site. No schema change (schema stays v21), `modal.type` 20 → 22 (`settings`, `wall`). Measured on the demo save at 390 px: home's `<main>` 1,076 px → 654 px, no longer scrolling; `NAV` is `grid-cols-5`. E2E churned 157 → 156 (Phase 1, the deleted home-card business step) → 157 (Phase 2, two new steps net of one dropped from the tab sweep losing `성장`); four mutation checks each failed exactly the intended step (reset wiring, stagnant-area routing, the squared bar formula, the CV's `자격` source), and a second, unrelated E2E defect was found and fixed the same day ([TD-39](../exec-plans/tech-debt-tracker.md)): a certification-search step was clicking the search `<input>` instead of a result button, so the main E2E save had silently declared no certification since the step was written. `PromoteModal`, `promoteArea`, `EvidencePicker`, `composeEvidence`, `RoleModelModal`, `roleGap`, `roleRecommendations`, `resetAll`, `exportBackup`, `importBackup`, `askImport`, `completeTask`, `tryComplete`, `BriefingModal`, `closeBriefing` and `migrate` are byte-identical. `docs/product-specs/growth.md` is retired to a redirect table; `docs/DESIGN.md`'s redesign invariants now read five tabs. | Rules 9, 10, 11, 13, 14, 16 · [completed plan](../exec-plans/completed/2026-09-15-cv-home.md) |
| 2026-09-16 | Assistant bridge kind guard. While assessing a CLOVA Note meeting workflow (not built — the user stopped at the analysis), a verifier found `parseAssistantReply` trusted a reply-declared `kind`: `{"title":"견적서 송부","kind":"book"}` imported as a reading task, bypassing the 2026-09-11 rule that a normal task must genuinely be reading or exercise. The kind now comes from `detectKind(title)` alone, the packet tells the assistant to name the activity in the title and no longer offers a `kind` field, and `importTasks` re-derives the kind and drops entries without one. No rule text changed; this enforces Rules 7, 18 and 19 as written. | Rules 7, 17, 18, 19 · [completed plan](../exec-plans/completed/2026-09-16-bridge-kind-guard.md) |
| 2026-09-16 | Six changes landed together, all from the user's own request, verbatim: `그럼 일단 미팅내용들을 다 저장하는 용도로만 쓰자 / 아무래도 api등의 유료 기능들이 많이 필요하니까 / 그리고 홈 페이지에 자격이랑 시험 뒤 D66 같은거 지워줘 / 시험도 정확한 점수 기재하게해 / 일정탭에 목록 지워주고 / 실행탭에 나열된거 제목이랑 디데이와 같은 핵심 내용들만 나열되게 해줘 / 그리고 클릭하면 상세내용 나오게끔 / 그리고 탭 이름을 좀더 어울리게 해줘`. (1) The home CV's `자격` and `시험` rows stopped printing `D{n}` after each name; the achievement wall keeps it (that sheet is the payout ledger, where D explains P). (2) Tabs renamed `프로필 · 목표 · 할 일 · 일정 · 미팅 · 사업` (internal `tab` keys unchanged; `IdCard` replaces `Flag` for `home`, `MessagesSquare` is new for `meetings`) and the `일정` tab became calendar-only — the `목록` view, its chips and the header's own `일정 추가` are gone, and `ui.scheduleView` is dropped at v22 since nothing reads it any more. (3) The `할 일` tab (renamed from `실행`) now shows one compact `TodoRow` per item — a lead chip, the title, at most one marker (`목표 기여 없음`, or a `Lock` on an open evidence-gated task) — with no checkbox; tapping opens `TaskDetailModal` / `EventDetailModal` / `BizTodoModal`, and completion is reachable only through `TaskDetailModal`'s `완료하기`, which still calls `tryComplete` exactly as the old row did. `DueChip` and `BizTodoRow` are deleted. (4) Exam milestones record the exact score from the score report (schema v22): `tasks[].score` and `exams.best[famId].score`, both optional display strings validated by `examScoreError` (numeric families must fall inside the band and at most the family's scale maximum; `opic`/`cambridge`/`jlpt`/`hsk` take free text) — `calcExamPayout` and the band snapshot are byte-identical, and a same-band retake updates only the shown score. (5) A sixth tab `미팅` (schema v23) stores `meetingProjects[]` and hand-written `meetings[]` grouped by project — a record only, capped (title 40 / attendees 80 / summary 800 / decisions 200 / actions 200 chars), guarded against the existing 3.5 MB storage budget with the form kept open on refusal, optionally linked to a `일정` event by id + date without copying its time; no payout, trophy, goal, streak or evidence gate, and neither the briefing, the to-do list, the assistant packet nor the calendar export reads it. (6) `tools/harness/gen-screenshots.js` now shoots `프로필`/`할 일`/`목표`/`일정`/`미팅`, closing [TD-41](../exec-plans/tech-debt-tracker.md) (`tasks.png`'s stale manifest label) and, in the same bench-script pass, [TD-40](../exec-plans/tech-debt-tracker.md) (`ab.js`/`perf.js`/`prof.js` still switching tabs by the retired `퀘스트` label). Two deviations from the plan, both load-bearing: the numeric-score test is `/^\d+(\.\d+)?(?=\s|$)/`, not the plan's original pattern, because that pattern also matched HSK's level label `1급`; `storageUsedWith(state)` was added because `storageUsedBytes()` alone runs one render behind the in-memory state the meetings guard needs to check against. **E2E was updated in every phase — new steps, renamed steps, new helpers (`openTodo`, `recordBoundary`, `submitPhotoEvidence`'s `score` option), and a new `flow10.js` for `미팅` — but not executed**, per the standing user instruction that the suite runs only when asked ([TD-44](../exec-plans/tech-debt-tracker.md)); every edited file parses (`node --check`). Two more debt rows recorded, not fixed: [TD-45](../exec-plans/tech-debt-tracker.md) (the meetings storage guard re-serialises the whole state on every check) and [TD-46](../exec-plans/tech-debt-tracker.md) (a deleted linked event leaves `eventId` dangling, by design — the view states the link is gone) and [TD-47](../exec-plans/tech-debt-tracker.md) (the task sheet's `삭제` still asks nothing, carried unchanged from the row's old `X` button) and [TD-48](../exec-plans/tech-debt-tracker.md) (`run.js`'s `assertDone` has a regex typo affecting only a failure message). | Rules 1, 2, 6, 7, 9, 10, 11, 12, 13, 16, 17, 18, 19 · [completed plan](../exec-plans/completed/2026-09-16-meetings-and-simplify.md) |
| 2026-09-16 | Done rows in place. The user reported `할일 탭 부분에 완료 처리 한것들의 목록이 안떠`; reproduced on the demo build, a completed task left its group for an `오늘 완료` section at the very bottom of a long list, where on a phone it looked like it vanished, and ticked schedule occurrences never reached the `완료` archive because it listed tasks only. The user chose the fix: `todoOf` keeps an item completed today in its own group as a struck-through `done` row after the open rows (a finished item is never `기한 지남`: a done daily task, a done task without a date, or one whose due date has passed sits in `오늘`; a ticked occurrence before today leaves the open list), `counts`, the empty states and the business-only note read open rows only, and the `오늘 완료` section and `todoOf`'s `done` return are removed (nothing else read them). The archive now lists completed tasks and every date in `ev.doneDates` as one newest-first list under `TODO_DONE_MAX`, and its count line totals both kinds; an event row opens `EventDetailModal` for that date. Completion still happens only through `tryComplete` in the task sheet; no schema change, no `migrate` block, no stored derived value. E2E (`flow5.js`, `flow7.js`, `run.js`'s `tapTodoRow`) updated but not executed, per the standing user instruction. | Rules 1, 9, 10, 11, 13, 16 · [completed plan](../exec-plans/completed/2026-09-16-done-rows-in-place.md) |
| 2026-09-16 | Meeting task links (schema v24). The user asked, verbatim, `회의록 작성은 할일목록들과 매칭 가능하게 해줘`, and chose, when asked, to link **existing** tasks (a meeting never creates one — tasks are still created only inside a goal) and to show the link on both sides. `meetings[].taskIds` holds up to 10 task ids (`MEETING_LIMITS.tasks`); the v24 block backfills `taskIds: []` on every meeting and changes nothing else. `MeetingModal` gains a `할 일 연결` picker (open tasks in `todoOf` order, then tasks completed in the last 30 days, a `할 일 검색` filter, a 30-row render cap, disabled rows and `할 일은 10개까지 연결돼요.` at the cap); `MeetingViewModal` lists `연결된 할 일` rows that open the task sheet and counts dangling ids as `삭제된 할 일 {n}건`; `TaskDetailModal` lists `관련 회의록` (derived by `meetingsOfTask`, hidden when empty) that open the meeting view — each replaces the other in the single modal slot. `removeTask`, and `removeGoal` for the open tasks it deletes (a deviation beyond the request, so links never dangle through either delete path), drop the ids from every meeting in the same clone update; `removeMeeting` changes no task. Linking pays nothing, completes nothing and moves no goal or streak; `parseAssistantReply` and the packet still do not read meetings. `meetingFits` already stringifies the whole record, so ten links (+132 chars) stay under the same budget guard. `demoState`'s `유지보수 범위 협의` links two demo tasks. E2E: five `flow10.js` steps and one `flow4.js` fixture step added, version assertions moved to 24 — updated but not executed, per the standing user instruction. | Rules 1, 9, 12, 13, 18, 19 · [completed plan](../exec-plans/completed/2026-09-16-meeting-task-links.md) |
| 2026-09-16 | Meeting form: the `할 일 연결` picker moves directly under the date, above the text fields — at the bottom it sat below the fold on a phone and the user reported the link feature as missing. Every completed task is now a candidate (the 30-day window and `MEETING_TASK_PAST_DAYS` are gone): minutes often concern work finished long ago. No schema change. | Rules 9, 13 · [record](../exec-plans/completed/2026-09-16-meeting-task-links.md) |
| 2026-09-16 | Meeting minutes caps widened at the user's request: summary 800 → 1,000, decisions 200 → 400, actions 200 → 400 characters (title 40 and attendees 80 unchanged). A completely full record grows from 1,510 to 2,110 chars (2,242 with ten task links); typical minutes (~850 chars) still fit three to five years at three meetings a working day, while completely full records at that rate would reach the storage budget in about two years and four months, where the existing save-refusal guard applies. No schema change. | Rule 13 · [meetings spec](../product-specs/meetings.md) |
| 2026-09-17 | Meeting minutes caps widened again at the user's request: summary 1,000 → 1,500, decisions 400 → 600, actions 400 → 600 characters. A completely full record is now 3,010 chars (3,142 with ten task links); typical minutes (~850 chars) are unaffected and still fit three to five years at three a working day, while completely full records at that rate would reach the storage budget in about a year and seven months, where the save-refusal guard applies. No schema change. | Rule 13 · [meetings spec](../product-specs/meetings.md) |
| 2026-09-17 | Meeting summary cap widened a third time at the user's request: 1,500 → 5,000 characters; decisions and next actions stay at 600. A completely full record is now 6,510 chars (6,642 with ten task links, about 17,050 with thirty full progress entries); typical minutes are unaffected, while completely full records at three a working day would reach the storage budget in about nine months, where `recordFits` refuses the save. The work packet is unaffected — it clips each summary at `WORK_PACKET_SUMMARY` = 200. No schema change. | Rule 13 · [meetings spec](../product-specs/meetings.md) |
| 2026-09-17 | Pasted AI replies no longer need the ```json fence: `replyJson` reads a fenced block, the whole text, or the first-`{`-to-last-`}` span, for both the daily bridge and the work bridge. The user's real reply, copied with a chat's code-block copy button, had read as no proposals. Validation is unchanged — the reply still only proposes, and only `tasks` / `work` are read. | Rule 7 · [assistant bridge](assistant-bridge.md) |
| 2026-09-17 | The work packet gets its own 20,000-char cap (`WORK_PACKET_MAX`) and carries each meeting's summary, decisions, follow-ups and up to five progress entries whole, plus whole work-item notes; a clipped text ends in `…`. The user found the old fixed clips (200 / 100 / 60 under the shared 4,000 cap) cut the minutes before the assistant could use them. The daily packet keeps 4,000. When over the cap, summaries shrink to 1,500 before the oldest meetings drop. | Rule 7 · [assistant bridge](assistant-bridge.md) |
| 2026-09-17 | The `업무` tab gains a select mode (`선택` → checkboxes, `전체 선택`, `선택 삭제 {n}건`, one confirmation) because deleting AI-imported items one sheet at a time was tedious. Scope is what is on screen — the day shown plus `지난 미완료` — never other days; no schema change. | Rule 9 · [daily work](../product-specs/daily-work.md) |
| 2026-09-17 | The work packet now states each visible meeting's attendees, linked schedule entry and linked tasks with their state (`연결된 할 일`), as the meeting view does, and carries up to 10 meetings instead of 6. The user found the linked tasks missing; the app never summarises — text is only clipped, with `…`, when the packet runs over 20,000 chars. A hidden meeting still gives date and title only. | Rule 7 · [assistant bridge](assistant-bridge.md) |
| 2026-09-17 | A work reply may propose up to 20 items (`WORK_PROPOSAL_MAX`, the per-day cap), up from 8, and the packet's rule 3 says `최대 20건`. One of the user's meetings alone had ten follow-ups, and the assistant stopped at eight because the packet told it to. Import still registers only what fits today's 20-item cap. | Rule 7 · [assistant bridge](assistant-bridge.md) |
| 2026-09-17 | No count limit on work items: the per-day cap of 20 and the reply's proposal limit are removed, and the packet's rule 3 says `건수 제한 없이`. The user carries undone items to the next day, so a day's list grows; only `recordFits` (the storage budget) bounds it. Supersedes the row above. | Rule 7 · [daily work](../product-specs/daily-work.md) |
| 2026-09-17 | Daily work (schema v25): meeting progress log, a seventh `업무` tab, and an AI-proposed work list through the bridge. The user asked, verbatim: `각 회의 후 어떤 업무를 이어서 진행했는지 진행사항 적고 모든일에 대한 요약을 해서 ai를 돌려 어플 내 모든 내용을 분석 후 오늘 할일을 만들어주고(오늘업무사항 탭 만듬) 수기로 추가도 가능하게끔 하고싶어`, then made four decisions when asked: (1) the AI runs through the existing copy/paste bridge, no key, no network; (2) the packet judges from goals, meeting minutes, progress logs and the rest of the app's facts; (3) a seventh bottom tab; (4) client meetings go into the packet **by default**, with a per-meeting `AI에 보내지 않기` opt-out — **reversing the 2026-09-16 decision** that the packet never reads a meeting at all, closing the backlog item "Whether the assistant packet may ever summarise meeting minutes" with this reversal. Design decision the main agent made on top: a work item is a new record kind, not a goal task — Rule 19's amendment restricts goal tasks to reading, exercise, certifications and study, so an item like `견적서 송부` cannot be one; `work[]` is a dated record (title, optional note, `done`, an optional link to a goal/meeting/project, `source: "manual"\|"ai"`) that pays nothing and never enters `computeGrades`, `krProgress`, `goalProgress`, `todoOf`, `buildBriefing` or an achievement log (Rules 1, 8, 9, 18). `meetings[].progress[]` (dated entries, capped `MEETING_LIMITS.progress` 300 chars / `MEETING_PROGRESS_MAX` 30, added and removed only in `MeetingViewModal`) and `meetings[].aiHidden` (a checkbox in `MeetingModal`, `AI에 보내지 않기`) are the two new meeting fields; a hidden meeting's work-packet line states only its date and title, never its project name. `NAV` gains `["work", "업무", ListChecks]` after `할 일`: `프로필 · 목표 · 할 일 · 업무 · 일정 · 미팅 · 사업`, `grid-cols-7` — the label reads `업무`, not `오늘 업무`, because four glyphs plus a space would clip a ≈ 51 px cell at 390 px. `WorkTab` (day pager, `지난 미완료` with `오늘로 옮기기`), `WorkModal` (one sheet for add/edit/complete/delete), and the second bridge packet `WorkBridgeModal` (`AI로 만들기 ›`, placed on the pager row rather than beside `업무 추가` because the label and both pager buttons together exceed the 326 px a 390 px screen leaves) round out the tab. `buildWorkPacket`/`parseWorkReply` are a second, independent packet and parser sharing `PACKET_MAX`, `packetSection` and `bizPacketLines` (both newly module-level) with the daily packet; a reply's `work` key is the only one read — `tasks`/`deals`/`events`/`meetings` are ignored entirely, never merged; a proposal becomes a record only once ticked, with `source: "ai"`; the raw reply is never stored (unlike `journal[].ai`). `meetingFits` is renamed `recordFits(next, prevLen, noun)`, now shared by meeting records, progress entries and work items under the one storage budget. Rule 7 gained a dated amendment recording this second packet and what a reply to it can and cannot do. Schema v25 (`work: []`; `meetings[].progress`/`aiHidden` backfilled), `modal.type` 28 → 30. E2E: new `flow11.js` (12 steps: 9 for the tab and progress log, 3 for the packet/parser/import), a `flow4.js` migration step, and the nav/schema-version assertions in `flow.js`/`flow4.js`/`flow6.js` moved to v25/seven tabs — written and parsed (`node --check`), **not executed**, per the standing user instruction. Two debt rows recorded, not fixed: [TD-49](../exec-plans/tech-debt-tracker.md) (a work item's link can dangle, same policy as TD-46) and [TD-50](../exec-plans/tech-debt-tracker.md) (the work reply is unrecoverable once dismissed, by design) and [TD-51](../exec-plans/tech-debt-tracker.md) (the packet's goal-lines block is duplicated under the finish gate's six-line window). | Rules 1, 7, 8, 9, 12, 13, 18, 19 · [completed plan](../exec-plans/completed/2026-09-17-daily-work.md) |
| 2026-09-17 | Secretary stage 1-A (schema v26): structured meeting follow-up items, derived carry-forward of undone work, a meeting-prep card, and briefing counts. Four user decisions: (1) `meetings[].followUps[] { id, text ≤ 200, mine (default off), due?, done, workId? }`, at most 30 per meeting; a `mine` item is registered automatically as a `source: "meeting"` work item (dated the meeting day when it is today or later, otherwise today) and stays linked both ways — only `done` mirrors, in either direction; turning `mine` off removes the linked item **only if it is still undone** (a done one stays linked); deleting the work item leaves the follow-up on the meeting, unlinked. (2) Carry-forward is derived, never stored: `workOn(state, today, today)` prefixes today's own view with every undone item from earlier days, oldest first, each stating `이월 {n}일`; the `지난 미완료` section, `workPastOpen`, `onMove` and `moveWorkToToday` are deleted, and a carried item still appears, struck through if done, on its own day — nothing is moved. (3) Events gain an optional `projectId` (`프로젝트 (선택)` picker in `EventModal`, listing `meetingProjects`); the meeting-prep card also matches an unlinked event by trimmed title equality against a previous meeting's. (4) Free-text `actions` is never rewritten by any of this — `MeetingViewModal` gains an `항목으로 나누기` button that splits it into candidates in an inline preview panel, appended only on confirmation. Planner defaults, none reopened: deleting a meeting does not cascade to its mirrored work items (the TD-49 policy); `modal.type` stays at 30 (no new modal — the split panel is component state inside `MeetingViewModal`). New pure helpers: `splitFollowUpText`, `followUpWorkDate`, `followUpWorkItem`, `reconcileFollowUps`, `meetingPrepOf`, `meetingRowMarker`, `workLeadOf`, `oneLineText` (lifted to module level). Root: `commitMeeting` (reconciles a meeting write against its mirrored work items and checks the storage budget for both together before writing either — the same render-state pattern `addMeeting` already used, TD-56), `toggleFollowUp`, `setFollowUpMine`, `appendFollowUps`; `toggleWork`/`removeWork`/`removeWorkMany`/`updateWork` gained the mirror-side of the link; `moveWorkToToday` deleted. The `업무` tab gains a `MeetingPrepCard` (`오늘 회의 준비`) as its first section, for every open schedule occurrence today and tomorrow that matches a meeting project; the briefing gains a `회의 준비` section (prep count, overdue follow-ups) and a first `오늘 할 일` line stating the carried count and oldest age; the work packet states each visible meeting's follow-ups as owner/due/state/text lines and its records section is retitled `업무 기록 (이월·어제·오늘)`. Schema v26 (`meetings[].followUps: []` backfilled; `events[].projectId` and `work[].link.followUpId` both optional, backfilling nothing). Three debt rows recorded, not fixed: [TD-54](../exec-plans/tech-debt-tracker.md) (a done follow-up's mirrored item can dangle its `followUpId` after the follow-up is removed), [TD-55](../exec-plans/tech-debt-tracker.md) (the prep card's title-match fallback is exact and case-sensitive), [TD-56](../exec-plans/tech-debt-tracker.md) (`commitMeeting`'s render-state reconcile pattern) and [TD-57](../exec-plans/tech-debt-tracker.md) (the shipped demo has no past-dated work item, so carry-forward is invisible there, only in `flow11.js`'s planted fixtures). E2E written across all three phases (14 new/rewritten steps, three schema-version assertions moved to 26), **not executed**, per the standing user instruction — static count 203 `await step(` calls. | Rules 7, 9, 12, 13, 18 · [completed plan](../exec-plans/completed/2026-09-17-secretary-stage-1a.md) |
| 2026-09-17 | Work items gain an optional `result` (`처리 내용`, ≤ 1,000 chars) at the user's request: how a finished item was done. Written in the work sheet (edit mode) and kept by `완료로 표시` itself; a done item's result goes into the work packet's records as `· 처리:` (≤ 300 chars). Optional field, no migration. | Rule 9 · [daily work](../product-specs/daily-work.md) |
