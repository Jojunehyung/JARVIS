# Tech-debt tracker
<!-- src: SPEC-11 -->

Severity: **S1** cannot run / data loss / wrong payout · **S2** behaviour contradicts a rule or doc, logic defect · **S3** doc residue, dead code, minor UX. Status: open · resolved (date, how) · accepted (allowlisted with reason). Allowlist entries in `tools/harness/finish-allowlist.json` must appear here.

## Open
| Id | Sev | Area | Issue | Note |
|---|---|---|---|---|
| TD-01 | S1 | goal engine | `krDoneCount` / `krProgress` (count) sum **every** task with the goal's `goalId`, including cert/exam/study `once` milestones → passing `전기기사` also advances `CATIA 30회` 2→3/30. Several count KRs on one goal share the same value (no per-KR link). | Needs a KR–task link key; migration block. |
| TD-03 | S2 | goals | `AddGoalModal` accepts start = target (instantly 100 %), count need ≤ 0, free-text cert names (bridge cannot match), past deadlines (instantly behind). Metric check-in blur on empty input stores `Number("") = 0`. | |
| TD-04 | S2 | dates | `dstr` is local-time dependent, so crossing time zones shifts the streak. | The off-by-one and the render-fixed `today` were fixed 2026-09-09 (`daysBetween`, and a `day` state re-read on focus and on a 60 s tick). |
| TD-05 | S2 | catalog | `CatalogModal` gain ignores job-fit weighting although the caption says otherwise. | |
| TD-06 | S2 | duplicates | Three different duplicate checks: `hasCertQ` (global partial match), `addTask` (exact), catalog owned (longest-name). `hasExamQ` is goal-scoped, so the same exam can be registered under two goals. | |
| TD-07 | S2 | payouts | A cert KR with tier C (×0) can still be registered (`+0P · 등록 ›`) and on completion logs "+0P" and a trophy. Re-taking an exam at a lower band pays 0 but still logs "+0P" and a trophy. Policy decision needed. | Rule 1/2 |
| TD-08 | S2 | goals | Cert KR auto-done scans all goals (ignores `goalId`); `applyMeasures` matches only the first KR per label with partial `includes`. Goal completion leaves no trophy/log; the done-goal path (`기록에서 제거`) still leaves every linked task's `goalId` pointing at nothing, with no cleanup. (The active-goal path, `목표 삭제`, is not this defect: it deletes the goal's record-less tasks and keeps `goalId` on the completed ones deliberately, so they render as ordinary `실행`-tab rows tagged `목표 기여 없음` — [Rule 18](../design-docs/core-beliefs.md#rule-18), [tasks.md](../product-specs/tasks.md).) | |
| TD-09 | S2 | study | `StudyVerifyModal` truncates insight (40) / critique (30) and does not keep the summary body. | Rule 16 |
| TD-10 | S2 | evidence | `resizeImage` 256 × 320 cover crop is applied to certificates/score reports → landscape documents become unreadable in the viewer. An aspect-preserving sibling, `resizeImageFit`, now exists and is proven by an E2E step against the business-tab portfolio image (2026-09-13), but `EvidenceModal` and `StudyVerifyModal` — the two evidence call sites — still call the original `resizeImage`, so a landscape certificate or score report is still cropped unreadable in `EvidenceViewModal`. | Switching the two evidence call sites to the aspect-preserving resize is [Rule 16](../design-docs/core-beliefs.md#rule-16) behaviour and deliberately left to its own plan, not folded into the business-tab plan that introduced `resizeImageFit`. |
| TD-11 | S2 | role model | No way to clear a role model (back to `null`); saving with every area excluded gives `roleAreas = null` but the button still says `롤모델 수정`. | Unchanged 2026-09-15 (home-CV rewrite), reworded 2026-09-18 (third role-model change of the day — no proximity figure exists to recompute any more): the `!areas` branch (`roleAreas(state) === null`) covers `RoleGradeSection`'s summary/expanded states (`요구 등급 없음 — 세부 수정에서 정해요`) and the briefing's `다음 단계` line, same as for no role at all; home's own row is driven by `roleStageOf`, independent of `roleAreas`, so a role with every area excluded but with stages still states its stage facts; `SettingsModal`'s button still reads `롤모델 수정` once any role is saved. See [home.md](../product-specs/home.md). |
| TD-12 | S2 | feedback | No overlay queue (consecutive effects are lost); `Overlay` effect depends on a new `onClose` each render (timer reset). Toasts overwrite each other. `gradeup` overlay ignores the uploaded photo. `TrophySvg` rank/spec ignore tier. | |
| TD-13 | S2 | onboarding | Re-selecting exams changes registration order → the dim decay lock lands on the last edited exam; language spec saved without the language area; `OptRow`/`Swatch` defined inside `Onboarding` (remount); no way back to the title from step 0; no feedback when `toggleDir` exceeds 3. | |
| TD-14 | S2 | streak | Completing the same daily task twice on one day is ignored silently. `applyDailyTick` handles only the monthly shield reset; during a gap the header 🔥 keeps the old value until the next completion. | naming vs behaviour |
| TD-15 | S2 | rule 2 | `exams.dim` is locked at completion (onboarding: at registration) while the rule says "first registration"; specialisation forms at D ≥ 75 but the floor applies from D ≥ 70; decay on upgrade deltas undocumented. | Clarify in scoring-engine.md |
| TD-16 | S2 | rule 4 | `ver` exists only on `exams.best`, not on `achievements`/`trophies`. | |
| TD-18 | S2 | rule 17 | Reading daily tasks ask for the log every time `evidence` is empty instead of one-tap after the first log. | |
| TD-19 | S3 | copy | `PromoteModal` `스스로에게 정직하게. 여기서의 상향은 결국 나를 속이는 일이에요.` is emotional copy (Rule 13 borderline). | |
| TD-20 | S3 | data | `CERTS.st` is informational only; `state.lastTick`/`dModel` written but never read; `EvidenceModal` `📎 사진 첨부` branch unreachable. | Allowlist candidates if `finish` flags them. |
| TD-21 | S3 | job fit | PMP/CISA/CISSP/CCSP sit in `클라우드·글로벌IT` → C for `기획·PM`; `한국사·물류관리사·유통관리사·직업상담사` judged on the `사무·회계` axis. Intersection ties pick the first `dir`. Areas without `지식` in the name and no `dir` get no recommendations. | Rule 6/15 review |
| TD-22 | S3 | data | D distribution skew in the original 174 (46 % between 50–69); `CAREER_OPTS`/`OUTPUT_OPTS` duplicate g values; `LEAD_OPTS.g` unused; onboarding-held non-sg certs can be re-earned later as milestones (onboarding pays no P, so impact is limited). | |
| TD-23 | S3 | scenario | Planning text "12→15 %" vs computed 3→5 %; `일일 D 프리필` vs code E; `실무자→중급` — no `중급` rank (`숙련자`); certificate completion does not change the role model's requirement-gap facts (only area grades do — and, since 2026-09-18, there is no proximity figure to recompute at all, [Rule 14](../design-docs/core-beliefs.md#rule-14)). | Kept as computed; see scenario spec |
| TD-24 | S3 | harness | E2E selectors are Korean UI strings — any copy change must update `tools/e2e` in the same plan. | By design; recorded |

| TD-25 | S2 | feedback | The RANK UP overlay always draws `PortraitSprite`, ignoring an uploaded profile photo; `TrophySvg` ignores `tier` for the `rank` and `spec` kinds, so the tier passed on goal completion has no effect. | |
| TD-26 | S2 | reset / input | `resetAll` runs without a confirmation dialog; after re-onboarding the component-level `imgs.profile` survives; an emptied metric check-in field stores `Number("") = 0` on blur. | Unchanged 2026-09-15: `데이터 초기화` moved from the growth tab into `SettingsModal` behind home's `설정` button; the root wires `setModal(null)` before `resetAll` so the sheet does not reappear after re-onboarding, but `resetAll` itself still asks nothing. |
| TD-27 | S3 | display | Multiplier prints as `×1` while the smoke text says `×1.0`; goal progress shows a truncated 10 % (10.5 mathematically); the completion toast drops 🔥 when it carries deltas; plain B/A task trophies use `legacyCertGrade(pts)` and land one tier low (B→C, A→B); `DiffBadge`/`CertBadge` emit `className "undefined"` for an unknown key and differ in their E tone (zinc-300 vs zinc-400); `AddTaskModal` falls back to `areas[0]` when the goal is undefined. | |
| TD-28 | S3 | doc drift | ~~The "one step below the requirement" proximity figure is quoted as ≈ 70 % in the docs and 60–70 % in the code comment, while the actual value ranges 44–77 % by requirement~~ (moot 2026-09-18, third role-model change of the day — the figure and its squared curve are retired entirely, [Rule 14](../design-docs/core-beliefs.md#rule-14) amendment; Rule 14's own text stays as history in `core-beliefs.md`); `profile.roleModel` is an unused legacy field; the `CERTS` header comments still describe the retired per-item score `s` and the pre-D grade letters. | The `profile`-shape half is resolved 2026-09-13: the `@schema` block now lists every field `profile` actually carries (`majorName`, `examsOwned`, `certs`, `career`, `lead`, `biz`, `output`, plus the new `name`/`birth`/`email`/`phone`/`edus[]`/`careers[]`), and `demoState.edu` is now the valid `EDU_OPTS` key `"ba"` instead of `"univ4"` ([decision-log](../design-docs/decision-log.md), [completed plan](completed/2026-09-13-cv-profile.md)). The proximity-figure wording is resolved 2026-09-18 (the figure it described no longer exists); `roleModel`/`CERTS`-comment issues above are unrelated and remain open. |
| TD-29 | S3 | unreachable | Recorded so a refactor does not reintroduce them: ~~`RoleGradeSection` (`RoleAdviceModal`'s body until 2026-09-18) has no guard for `rg === null` — printing `모든 요구 영역을 충족했습니다. 근접도 undefined%.` if it ever rendered with one~~ (fixed 2026-09-18, third role-model change of the day — `RoleGradeSection` now guards `!areas` with `요구 등급 없음 — 세부 수정에서 정해요`, and the sentence it would have malformed no longer has a `근접도 {match}%.` tail to malform); no bound check on `RANKS[have + 1]` (requirements cap at 8, gaps filter `gap > 0`), and recommends the next exam band without the gain filter certifications use (payout is always ≥ 1 because of the 0.3 decay floor). `areaCatHints` returns no category for a custom area with no direction, and `jobWeightForCert` breaks a tier tie by direction order (documented in [../design-docs/job-weighting.md](../design-docs/job-weighting.md)). **History (unreachable-branch note, kept):** until 2026-09-15 the `rg === null` branch was unreachable because every entry point was itself gated on `rg` existing; the 2026-09-18 role-screen rewrite (second change of the day) added one path where it was not — the `롤모델` screen's `영역 등급` section renders its `펼치기 ›` toggle regardless of `rg`, so a role saved with a story but no requirement grades (the CV's third state that day) could open `RoleGradeSection` with `rg === null` and hit the then-unguarded branch; found during that change's docs-sync pass, not fixed there — fixed by the guard added the same day, third role-model change. | |
| TD-30 | S3 | schedule | `HOLIDAYS` covers 2026-2027 only. From 2028-01 the month grid marks no public holiday and shows `공휴일은 2026~2027년만 표시해요.` under it instead. | Extend through 2028 before 2027-12-31, from the government announcement published during 2027. The coverage note, the note's years and the E2E step "a month outside the table marks no holiday" all follow the table, so only the rows are added. |
| TD-31 | S3 | task creation | After `채우기 ›` prefills the daily form from a count KR, the title field stays editable and `krId` survives an arbitrary rewrite, so one kind-less task per count KR can still be created deliberately by typing over the prefilled title. | Accepted — the goal owns the count KR either way ([Rule 19](../design-docs/core-beliefs.md#rule-19) amendment). |
| TD-32 | S2 | E2E harness | `openTaskModalFor(goalTitle)` in `tools/e2e/run.js` filters `document.querySelectorAll("div")` for one containing both the goal title text and an `실행` button, then clicks the **last** match — but the last such match is the `진행 중` section wrapper (it contains every active goal's title and every goal's button), not the specific goal's own card, so the helper always opens the first goal card's `＋ 실행 연결` regardless of `goalTitle`. Proven with a puppeteer probe during the 2026-09-11 goal-task-kinds implementation. Consequence: `flow2.js`'s `기술서 30분 읽기` and `flow3.js`'s `웨이트 40분` are registered under `어학 점수 확보`, not `하네스 설계 엔지니어 취업` as their step names claim — the steps still pass (the task is created and completed somewhere), so no E2E step is currently red, but the goal-progress assertion they imply is weaker than it reads. | Not fixed here. Fix means scoping the search to the innermost matching card (e.g. stop at the first ancestor whose own direct text, not `innerText`, contains the title) rather than picking by array position. Re-read 2026-09-13 for the unified to-do list plan: unaffected — `openTaskModalFor` still opens `목표`'s own card structure, which that plan does not touch. |
| TD-33 | S2 | storage | `STORAGE_BUDGET` (3.5 MB) is a heuristic ceiling guessed for a slice of the whole `localStorage` origin, which the state save also shares; `persisted` cannot tell a genuinely full quota apart from a browser that blocks `localStorage` outright (private mode, disabled site data), so the latter surfaces the `저장에 실패했어요` toast on every single state change instead of once. | Needs a real capability probe at boot, not a per-write byte-count guess. |
| TD-34 | S3 | business | `deals[].months` / `startMonth` describe the **billing** period only; the underlying work period (when the engagement was actually delivered) is not stored anywhere, so a 3-month-billed contract that took 5 months to deliver cannot be told apart from one where they matched. | By design for now — the tab only needed to state what is billed and collected. Add a separate field if the work period is ever asked for. |
| TD-35 | S3 | business | `FolioView`'s thumbnail effect re-reads every `liferpg-img-folio-{id}` key on every mount, so leaving `포트폴리오` and coming back rescans all of them again — cheap at a handful of entries, not necessarily at the storage budget's practical ceiling. | Cache across mounts (e.g. lift the map into a ref owned by `BizTab`) if the portfolio grows large enough to notice. |
| TD-36 | S3 | business | A deal carries no `rateId` and a portfolio entry no link back to the deal it came from. Both are deliberate: a deal snapshots its own `monthly` / `costMonthly` at signing so a later rate edit cannot rewrite a past contract's numbers, and a portfolio entry is written once a piece of work exists, independent of which contract (if any) it was ever billed under. | Accepted — recorded so a future refactor does not "fix" this into a foreign key by mistake. |
| TD-37 | S3 | E2E harness | `flow.js`'s `step 3 — areas and knowledge directions` clicks `기본지식` and `커리어`, but `Onboarding`'s area chips start selected (`useState(["사업", "직업·커리어", "기본지식"])`), so the click **deselects** them instead of adding them — the step does the opposite of what its name claims. Onboarding proceeds with exactly one area, `사업`, silently narrowing every downstream fixture built on the main E2E save. Found 2026-09-13 writing the `growth-simplify` growth-tab step, which needed a real area name and got `사업` instead of the expected `기본지식`. | Not fixed here — out of scope for `growth-simplify`. Fix means asserting the intended selected set (e.g. reading each chip's pressed state) instead of trusting click direction, or picking a fourth, unselected preset instead of these two. Unchanged 2026-09-15 (home-CV rewrite): the new `CV grade rows are one line each and open the promotion gate` step in `flow2.js` reads its target area's name from the save rather than hard-coding `사업`, so it stays correct however many areas onboarding actually selects — but the onboarding-step-3 defect itself is still not fixed. |
| TD-38 | S3 | calendar export | A calendar file is a snapshot: an edit, a completion or a deletion made in the app after export never reaches an already-imported calendar entry, a completed item keeps alarming until the user removes it by hand in the calendar app, and the daily digest's task list is frozen to whatever it was at export time. | Accepted — inherent to a file export with no server ([Rule 7](../design-docs/core-beliefs.md#rule-7)); recorded so a later plan does not promise live sync. Whether re-import replaces, skips or duplicates a UID, whether a calendar app honours the file's `VALARM`s, and how it reads the file's floating local time all depend on that calendar app — see [../design-docs/calendar-export.md](../design-docs/calendar-export.md) and the pending manual device check in [../product-specs/install-and-backup.md](../product-specs/install-and-backup.md) (backlog item 2). |
| TD-42 | S3 | CV | The home CV's `자격` row and `ProfileModal`'s `보유 기록` both list `profile.certs` (declared at onboarding, never proven) and done `isCert` tasks (proven with a certificate photo) in one merged, sorted-by-D list (`heldCertsOf`) with no marker telling the two apart — a name typed in at onboarding reads identically to one paid for and photographed later. | Accepted 2026-09-15 — the list existed at all only for the earned half before this plan (`profile.certs` was never counted on its own screen); merging in the declared half without a marker was the deliberate, smaller change. A `자기신고`/`증거` tag per entry would need `heldCertsOf` to carry provenance, not just `{ n, d }`. |
| TD-44 | S2 | E2E harness | The 2026-09-16 `meetings-and-simplify` change edited `tools/e2e` in every one of its five phases (tab renames, the compact `할 일` rows and their detail sheets, the calendar-only `일정` tab, the exact exam score field, and the new `flow10.js` for `미팅`) under a standing user instruction that the suite runs only when the user asks. Every edited file parses (`node --check`), but none of it — old steps or new — has been executed since the edits landed. The 2026-09-17 `daily-work` change added thirteen more steps under the same instruction (twelve in the new `flow11.js`, one in `flow4.js`) and widened the nav/schema-version assertions in `flow.js`/`flow4.js`/`flow6.js`; none of it has been run either. The 2026-09-17 `secretary-stage-1a` change (schema v26) edited the suite across all three of its phases — Phase 1 added six `flow10.js` steps and one `flow4.js` fixture step and moved four files' version assertions to 26; Phase 2 rewrote and added `flow11.js` steps for carry-forward and extended the demo-sweep counts assertions; Phase 3 added one `flow7.js` step and one more step each in `flow10.js` and `flow11.js`, and moved `flow10.js`'s closing cleanup into its last new step — none of it has been run either; the static count was 203 `await step(` calls at that point. The 2026-09-17 `urgent-memo-transcripts` change (schema still v26) added five more `flow10.js` steps and two more `flow11.js` steps (project-less memos, the transcript field, the work packet's memo/transcript exclusion, `회의록 열기`), changed `plantMeetingRecord` to keep an explicit `projectId: null`, and extended `flow.js`'s demo sweep — also not run; the static count was then **211** `await step(` calls. The 2026-09-17 `documents-prep-checks-daily-reader` change (schema v27) edited the suite across all three of its phases — Phase 1 added five steps (documents, four in `flow10.js` and one in `flow4.js`) and moved four files' version assertions to 27; Phase 2 added five `flow11.js` steps (pre-meeting checks, the prep packet, the prep reply, `확인할 것 가져오기`) and changed the prep step to tap the prep card's new `회의록 열기 ›` button instead of the whole block (the block became a `div`, TD-64); Phase 3 rewrote three `flow5.js` steps and added one more (the daily reader replacing the briefing's auto-open) and one `flow11.js` step — none of it has been run either; the static count was then **223** `await step(` calls. The 2026-09-17/18 `business-tracks-roadmap-role-stages` change (schema v28) edited the suite across all six of its code phases — tracks (seven steps across `flow4.js`/`flow7.js`/`flow8.js`/`flow10.js`/`flow11.js`), the roadmap (three `flow8.js` steps), time budget and payments (two `flow11.js` + two `flow8.js` steps, plus a `flow6.js` backup extension), role stages (two `flow3.js` steps, one `flow2.js` assertion), pipeline and notices (three `flow8.js` steps), and calendar alarms plus the `주간 회고` packet (two `flow9.js` + two `flow5.js` steps, plus assertions inside an existing `flow11.js` step) — none of it has been run either; the static count was then **246** `await step(` calls. The 2026-09-18 `role-model-story-verdict` change (schema stays v28) edited the suite across both of its code phases, also not run: Phase 1 added two `flow3.js` steps (the fifth packet's story/CV/grades/counts/stages with no identifier; a pasted verdict reply's rejects and confirmed save) and extended the stage-editor step to plant and assert a survived story and older verdict; Phase 2 added two more `flow3.js` steps (the headline's progress percentage moving with records while the proximity figure stays; the once-per-stage completion overlay) and one `flow5.js` `READER_SECTIONS` entry, plus the demo-sweep headline/caption/reader assertions in `flow.js` — the static count is now **250** `await step(` calls. The 2026-09-18 `role-model-screen` change (one `롤모델` screen, third change of the day per `tools/e2e/README.md`) edited the suite in its one code phase, also not run: five new `flow3.js` steps (story templates and the story-only save; the storyline's done/current/upcoming stages and each unmet condition's landing on its view, sheet or catalogue; the current stage's linked work items and their sheet; `롤모델 초기화` asking once and removing only `role`; the briefing's `다음 단계` line and the reader's `롤모델 판정 열기` both opening the screen), six rewritten `flow3.js` steps (the proximity line, the stage step, the editor step through `세부 수정 ›` with `기본 9단계 채우기`, the verdict-CTA regex, the verdict save's return to the screen), and edits to `flow.js` (settings step, no-role briefing step, demo sweep) and `flow2.js` (the role step through `세부 수정 ›`) — the static count is now **255** `await step(` calls (`flow3.js` 20 → 25). The 2026-09-18 `role-model-no-percent` change (fourth `롤모델`-related change of the day counting only role-model-specific edits as third, schema stays v28, the [Rule 14](../design-docs/core-beliefs.md#rule-14) amendment) edited the suite in its one code phase, also not run: every `%`-asserting step in `flow.js`/`flow2.js`/`flow3.js` was rewritten to assert no percentage anywhere on home or the `롤모델` screen, `flow8.js`'s quit-text helper was corrected in passing (see [TD-94](tech-debt-tracker.md)), and the static count stays **255** — every rewrite replaced an existing assertion rather than adding or removing a step. | Open, by design — the first run is pending the user's own `npm run verify`. Not a defect in the app; recorded so no later plan assumes the suite is green because it reads green in the source. |
| TD-45 | S3 | reliability | The storage guard (`recordFits`, renamed from `meetingFits` 2026-09-17 and now shared by meetings, progress entries and work items) and the `미팅`/`업무` tabs' storage-use lines both call `storageUsedWith(state)`, which `JSON.stringify`s the **whole** state on every save and on every render of a counts line, rather than tracking a running total. Cheap today; at a save nearing the low megabytes this becomes a measurable stringify cost on a path a user hits by typing, not just by adding evidence photos. | Not fixed here — measure the stringify cost at a ~2 MB save before deciding whether a running byte counter (updated incrementally on write) is worth the added bookkeeping. |
| TD-46 | S3 | meetings | Deleting the `일정` event a meeting links to (`eventId`) never cleans up the link: the minutes survive, unchanged, and `meetingEventText` reads the dangling id and states `연결된 일정이 삭제됐어요` at render. This is the documented, deliberate behaviour ([meetings.md](../product-specs/meetings.md)) — a meeting is a record and is never silently edited by something else deleting — but it means `eventId` can point at nothing indefinitely. | Accepted — recorded so a later "cleanup" pass does not treat the dangling id as a bug to null out automatically; the live lookup already degrades gracefully. |
| TD-47 | S3 | tasks | `TaskDetailModal`'s `삭제` button calls `onRemove(q.id)` → `removeTask` with no confirmation dialog, exactly as the row's own `X` button did before the 2026-09-16 rewrite moved deletion into the sheet — the behaviour is carried forward unchanged, not introduced. | Not fixed here — out of scope for a rewrite that moved existing controls into a sheet without changing what they do. A confirmation would be a product decision, not a refactor. |
| TD-48 | S3 | E2E harness | `tools/e2e/run.js`'s `assertDone` builds its failure message with `txt.replace(/s+/g, " ")` — missing a backslash, so it is `replace(literal "s" one-or-more, " ")` rather than `replace(/\s+/g, " ")` (collapse whitespace). It only degrades a thrown message's readability; the pass/fail check above it is unaffected. Pre-existing, found while documenting the 2026-09-16 `completeQuest`/`openTodo` rewrite that this function sits beside. | Not fixed here — one-character fix (`s+` → `\s+`), left for a harness-only pass since this plan does not touch `run.js` logic beyond the helpers it needed. |
| TD-49 | S3 | daily work | A work item's `link` may point at a goal, meeting or project deleted afterward; `workLinkText` states `연결 대상이 삭제됐어요` at render and it is never cleaned up — `removeGoal`, `removeMeeting` and `removeProject` do not touch `work`. Same policy as TD-46's dangling `eventId`. | Accepted — a work item is a record and is never silently edited by something else deleting; the live lookup already degrades gracefully. |
| TD-50 | S3 | assistant bridge | The work reply's raw text is not stored anywhere (unlike the daily bridge, which writes `journal[].ai`), so a mis-tick in `WorkBridgeModal`'s confirm view cannot be recovered without re-pasting the same reply. | Accepted — storing it would need a second journal-like slot and would collide conceptually with `journal[].ai`; nothing derived needs to survive a reload here, and the packet itself is reproducible on demand. |
| TD-51 | S3 | assistant bridge | `buildAssistantPacket`'s goal-lines block (title, deadline/D-day, progress, pace, up to four KR remainders) is duplicated almost verbatim in `buildWorkPacket` — five source lines, under `finish`'s six-line duplicate window, so the gate did not flag it. | Not fixed here — extracting a shared `packetGoalLines(state, today)` (mirroring the 2026-09-17 `packetSection`/`bizPacketLines` extractions) is a small, low-risk follow-up left for a future pass since it is not required for correctness. |
| TD-52 | S1 | assistant bridge | Both reply parsers read only a fenced `json` code block. A chat's code-block copy button copies the JSON without the fence, so a correct reply pasted that way showed `제안 업무 없음 — 등록할 항목이 없어요.` (reported by the user on 2026-09-17 with a real ChatGPT reply of eight proposals). | Resolved 2026-09-17 — shared `replyJson` reads a fenced block (tagged or not), else the whole text, else the first-`{`-to-last-`}` span; every field is still validated by the callers. flow11 step written, not run. |
| TD-53 | S1 | assistant bridge | The work packet clipped every meeting at fixed caps (summary 200, decisions, follow-ups and progress 100, three progress entries, work-item notes 60) under the daily packet's 4,000-char cap, so the assistant saw only the first lines of minutes the user can write at 5,000 chars (reported by the user on 2026-09-17 with a real packet). | Resolved 2026-09-17 — own cap `WORK_PACKET_MAX` 20,000; summary, decisions, follow-ups, progress (5 entries) and notes go whole; a clipped text ends in `…`; the trim order first cuts summaries to 1,500, then drops the oldest meetings. The daily packet keeps `PACKET_MAX` 4,000. |
| TD-54 | S3 | meetings | A follow-up removed in `MeetingModal` while its mirrored work item is still done leaves that item's `link.followUpId` pointing at nothing — `reconcileFollowUps` only takes a removed follow-up's work item with it when the item is **undone**; a done one stays, its meeting link then dangling. The item's `연결` line still states `회의록 · {date} {title}` (the meeting itself), so it never looks unlinked from the user's side. | Accepted — the same TD-49 policy (a work item's link is a reference only, never silently edited by something else). |
| TD-55 | S3 | meetings | `meetingPrepOf`'s title-match fallback (an event with no `projectId` whose trimmed title equals a previous meeting's) is exact and case-sensitive; retitling either the event or the meeting after the fact stops the match, silently dropping the prep block for that occurrence. | Accepted — `projectId` is the reliable path; the title match is a convenience for events created before a project link existed. |
| TD-56 | S3 | meetings | `commitMeeting` reconciles follow-ups against the render's `state.work`, not an updater's `prev.work` — the same pattern `addMeeting` already used to build its own record before this change. Two writes to `meetings`/`work` inside one render tick (e.g. two rapid follow-up toggles) could race and lose one's effect. | Accepted — every write here is user-driven (a button tap, a form submit), so two writes landing in the same tick is not a realistic path; recorded so a later automation (e.g. a bulk import) does not assume it is race-free. |
| TD-57 | S3 | demo data | `demoState`'s `work[]` has no item dated before today, so the demo build shows no carried row and no `이월` counts fragment — the derived carry-forward feature (schema v26) is exercised by `flow11.js`'s planted fixtures, never by the shipped demo. | Still open — the schema v27 plan added a **yesterday-dated, done** work item with a `result` (`○○물산 월 리포트 양식 회신`) so the daily reader's `오늘 업무` section states a `처리:` line, but it is not undone and before today in the sense this row names: it is done, so it is not in today's carried view and the counts line (`남음 2건 · 이월 0건 · 완료 1건 · AI 제안 1건`) is unchanged. An undone, past-dated item is still left for a future demo-data pass. |
| TD-58 | S3 | daily work | `회의록 열기` (2026-09-17) on a meeting-linked work item opens `MeetingViewModal` in place of `WorkModal`, since the root has a single modal slot; closing the view lands on the `업무` tab underneath, not back in the work sheet, and anything typed in the sheet and not saved is dropped — the same as closing the sheet any other way. | Accepted — a return path would need a modal stack, out of scope for this change; the user re-opens the row from the tab. |
| TD-59 | S3 | storage | `STORAGE_BUDGET` (and the whole `recordFits`/`storageUsedWith` guard) counts string length, while browsers meter `localStorage` in UTF-16 code units, so the guard admits roughly **121** full 30,000-char transcripts where the physical per-origin quota holds roughly **55–60** — the same string-length-vs-UTF-16 gap TD-33 already named for the budget as a whole, now sharper because a single optional field (`meetings[].transcript`, 2026-09-17) can be 30,000 chars on its own. | Accepted — pre-existing heuristic (TD-33); the `미팅` tab's `저장 공간` line and the backup path are the mitigations, same as before. A real capability probe (TD-33's own proposed fix) would close this too. |
| TD-60 | S3 | meetings | `meetingPrepOf`'s title-match fallback (TD-55) names the **newest** meeting with the event's trimmed title, whatever its project — so if that newest same-title meeting is a project-less memo (2026-09-17), the event gets no prep row even when an older same-title meeting names a live project. | Accepted — recorded alongside TD-55; a fix would match the newest same-title meeting that **has** a project, skipping memos, left for a future pass since `projectId` on the event is the reliable path either way. |
| TD-61 | S3 | schedule | `events[].checks` (schema v27) lives on the event record, not on an occurrence, so a repeating event carries the same checklist to every occurrence — a check ticked this week is still ticked next week until the user unticks or deletes it. | Accepted (planner default) — an occurrence-keyed checklist would need a date map on the event for a use the user has not asked for. |
| TD-62 | S3 | daily assistant | The daily reader's `readerSince` falls back to **yesterday**, not today, on a second opening the same day, because the one marker (`act.briefingSeen`) is stamped at close, so a same-day reopen cannot tell "closed once already today" from "never opened" without reading the marker as if it were before today. | Accepted — the section's own title states the date it counts from (`{since} 이후 새로 들어온 것`), so the window is never a silent gap; a second per-day marker was rejected by the user's own decision against storing anything extra per day. |
| TD-63 | S3 | meetings | `dealsOfProject(state, project)` matches a deal's trimmed `client` as a **substring** of the project name — no stored link (`clientId`) between a project and a deal. A project and a deal that happen to share a short client name (e.g. a common surname) would match incorrectly; a client renamed on one side but not the other stops matching. | Accepted — a `clientId` field is a schema addition for a heuristic that works on the names the user already types; revisit if a false match is ever reported. |
| TD-64 | S3 | daily work | The meeting-prep card's block (`MeetingPrepCard`) became a `<div>` at schema v27, with an explicit `회의록 열기 ›` border button, because the v27 checklist's own checkboxes and inputs cannot nest inside the `<button>` the block used to be. `tools/e2e/flow11.js`'s prep step was updated to tap the new button. | By design, not a defect — recorded because it changes an E2E selector and a screenshot-adjacent interaction pattern (a whole-card tap no longer opens the meeting). |
| TD-65 | S3 | daily assistant | `buildPrepPacket` / `buildWorkPacket`'s shared `meetingPacketLines(…, { openOnly: true })` decides whether to print per-item follow-up lines or the raw `actions` text by whether the meeting has **any** follow-up items at all, not whether it has any **open** one — a meeting whose items are all done still gets the per-item lines (all marked `완료`), never the raw-text fallback, even though every item is closed. | Accepted (implementer note, Phase 2) — stated in code as intentional (`openOnly`'s own comment); a meeting with all-done follow-ups is rare enough that a wrong fallback (which would print raw text alongside closed items) is worse than the current behaviour. |
| TD-66 | S3 | daily assistant | `readerSince`'s three cases are read entirely off `act.briefingSeen`; there is no fallback to a specific meeting's date — the fallback window (`READER_SINCE_FALLBACK_DAYS`, 7 days) is a fixed constant, not derived from any record. A save whose last activity was more than 7 days ago (a returning user) sees only the last week's arrivals in `{since} 이후 새로 들어온 것`, not everything since their last visit. | Accepted — the marker (`act.briefingSeen`) is the only per-visit signal the app stores; deriving a longer fallback from, say, the oldest unread meeting would need a second signal the user has not asked for. |
| TD-67 | S3 | E2E harness | `tools/e2e/flow11.js`'s daily-reader step (Phase 3) must run **before** the prep-checks import step in the same file, not after: registering the meeting `확인할 것 가져오기` writes a newer meeting on the project, which would change the prep card's `마지막 회의` and its `결정:` line before the reader step can assert them. The ordering is load-bearing and undocumented in the step itself beyond a comment. | Accepted, recorded so a future edit to `flow11.js` does not reorder these two steps without re-reading both. |
| TD-68 | S3 | meetings | A project-less memo defaults to the `직장` (day-job) track (schema v28) and, until the user flips it, drops out of every AI packet — including one the user meant as a personal or business note. | Accepted (the planner's stated safe default) — a record wrongly left on `직장` is only absent from a packet, never leaked into one; flipping a memo's track chip is one tap. |
| TD-69 | S3 | business | `milestonePace` reads linked work only: a milestone with no `workIds` states `연결 업무 없음 — 페이스 계산 불가` rather than any number, even when its `due` has clearly slipped. | Accepted — pace needs a progress signal, and the app has none for a milestone beyond its own linked work; the D-day line still states lateness on its own. |
| TD-70 | S3 | business | Roadmap stage order is **stated, never enforced**: an `active` milestone above the lowest not-done stage only triggers the amber `단계 순서: {a}단계 미완 · {b}단계 진행 중` note — nothing blocks registering or activating it. | By design (the user's own decision) — sales can start while delivery still runs; the note is the only guard. |
| TD-71 | S3 | daily assistant | `weekFacts` (the weekly review's per-track facts, v28) reads due dates and item dates rather than a completion stamp, because a follow-up and a work item store `done` only, with no "completed on" date. A follow-up due last week but finished this week, or a work item dated last week but finished today, is counted in the wrong week or not at all. | Accepted — adding a completion-date stamp to every follow-up and work item is a schema change beyond this plan's scope; the review states what dates the app already has. |
| TD-72 | S3 | business | A payment line, a milestone, a lead and a notice have no `todoOf` row at all — the briefing and the reader carry them instead, never the unified `할 일` list. Separately, the briefing's `biz` section alerts (unpaid months, payment lines, an overdue-lead count, near notices, milestone lines) share one `CAP − 1` budget with the pre-existing unpaid-month/ending-contract/stale-quote alerts, so a full slate of business alerts can push an ending contract, a stale quote or a milestone line out of the briefing itself — and so out of the daily packet, which reads the briefing's lines. | Accepted — a seventh business row kind on `할 일` was rejected in favour of the existing briefing/reader surfaces; the crowding is the same `CAP` trade-off the section has always made, extended to five more alert kinds, and the closing revenue line is never the one dropped. |
| TD-73 | S3 | meetings | There is no project-level (or record-level) override that lets a `직장`-track record reach an AI packet anyway — flipping the record's track, or (since 2026-09-22) the global `settings.workInAi` switch, are the only two controls; still no per-record "send this one anyway" toggle. | By design; recorded as a backlog item if the user asks — a one-tap override would sit one tap away from sending confidential day-job data to an external chat. |
| TD-74 | S3 | e2e | The demo's day-job project (`데이터 프로파일링 — 데모기관`, v28) moved several demo-sweep assertions: the project/meeting/document counts, the `업무` tab's prep card and counts line, and the briefing's prep line all changed once the day-job records were added. | Accepted — recorded so a future doc or E2E edit does not "restore" the pre-v28 demo figures by mistake; the new figures are the correct ones. |
| TD-75 | S3 | business | `ROADMAP_SEED` carries **absolute** dates from the user's own 2026-09-17 situation (through 2027-12-31), so a demo run after that date shows every seeded milestone overdue. | Accepted — the seed is data the user edits in the app; a relative-date seed would need to recompute the "situation" narrative instead of quoting it. |
| TD-76 | S3 | daily assistant | `parseWorkReply`'s duplicate check (unchanged since it was written for `오늘 업무 만들기`) compares a proposed title against **today's** view (`workOn(state, today, today)`), not against next Monday's list — the date `주간 회고`'s reply actually imports to. A proposal whose title already exists on next Monday is not refused by the parser; only the packet head's own rule 3 ("don't repropose what `미완료 업무·후속` already lists") guards against it. | Accepted — changing the shared parser's dedupe window for one caller would need a date argument threaded through a function three packets share; the packet head's own instruction is the guard for now. |
| TD-77 | S3 | calendar export | A daily-repeating event with open checks writes one check-reminder entry per included occurrence — up to 365 at the one-year export range — because a checklist belongs to the event record (TD-61) and every occurrence needs its own day-before reminder. | Accepted — the same trade-off TD-61 already made, extended to the calendar file; a once-per-event reminder would need to know which occurrence the checks are "for," which the app does not track. |
| TD-78 | S3 | business | `leads[]` and `notices[]` (schema v28) carry no `track` field at all — both are business by nature, so there was nothing to default. Neither the work packet nor the prep packet reads them either way; only the reader (never a packet) lists them, and the `주간 회고` packet includes them unconditionally. | By design — recorded so a later plan does not add a `track` field expecting one of `work`/`personal` to be reachable; a lead or a notice is always `사업`. |
| TD-79 | S3 | daily assistant | The daily packet's `회의 준비` count line (`오늘 회의 준비 {n}건 · 직장 {a} · 사업 {b} · 개인 {c}`) states the per-track breakdown even though the line's own severity-based inclusion rule (`briefLines`) has no `track` field to filter on — a day-job count, never a title, reaches the packet through this one line. | Accepted (Phase 1 coordinator fix) — a count is not a leak the way a title or a record would be; the alternative (dropping the per-track counts from the packet-visible line while keeping them on the briefing) would state two different counts for the same line depending on which screen reads it. |
| TD-80 | S3 | business | `milestoneLine`, the roadmap row marker and the milestone sheet's D-day fact all read `ddayStr`, which is anchored to the device clock (`Date.now()`), not the `today` argument the rest of the roadmap machinery (`milestonePace`, `elapsedBetween`) takes — a device with a wrong clock, or a screenshot/export frozen at a past `today`, would show a D-day inconsistent with the pace figure beside it. | Accepted (Phase 2 implementer note) — `ddayStr` is a pre-existing, widely shared helper (used by exams, goals and events); giving it a `today` argument for one caller was out of this plan's scope. |
| TD-81 | S3 | role model | `role.seenStageK` is stamped silently the first time a save is rendered after this change lands (no stamp → the effect stamps the current `k` without raising the overlay) — a stage that completed **before** the change shipped, whose derived `k` already sits past the stamp's implicit starting point, never shows the completion overlay retroactively. | Accepted — the seen-stamp is defined only from the point it exists forward, exactly as `act.briefingSeen` never retro-fires the daily reader for days that predate it; a backfilled stamp would need to know which stage was "already" complete before the field existed, which the save does not record. |
| TD-82 | S2 | role model / security | The role verdict packet's `## 이력` line states the CV's latest career role **title** (`cvSummaryOf`, the same line every packet states) and `role.story` is free text the user types with no app-side scan for identifying content — the textarea's caption (`이름·연락처는 적지 않아요.`) is the only guard against the user typing an employer name, a client name or another identifier into the story itself. | Accepted — every other packet already states the same CV line by the same rule ([SECURITY.md](../SECURITY.md)), and scanning free text for names the app has no dictionary of is out of scope; the caption is the guard, as it is for every other free-text field in the app. |
| TD-83 | S3 | job weighting | `parseRoleVerdictReply`'s `cert_held` normalisation (`certByTitle(arg)`, longest-name-first substring matching, [Rule 15](../design-docs/core-beliefs.md#rule-15)) accepts an AI-given name that merely **contains** an official certification name as a substring, normalising it to the longest match — the same pre-existing matching behaviour every other `certByTitle` caller has, now reachable from a pasted AI reply rather than only from typed evidence text. | Accepted — this is `certByTitle`'s documented behaviour, unchanged by this plan (rule 15's own longest-match design); a stricter caller-specific match would diverge from every other certification lookup in the app. |
| TD-85 | S3 | docs | The exec plan's Rule 7 amendment (2026-09-18, `docs/design-docs/core-beliefs.md`) states "the four earlier parsers are unchanged," but only three parsers precede this one — `parseAssistantReply`, `parseWorkReply`, `parsePrepReply`; the fourth packet, `buildReviewPacket`, reuses `parseWorkReply` and has no parser of its own (phase 2 progress note, plan error found during implementation). | Not fixed here — the amendment paragraph is landed, dated, user-approved text; correcting the count would mean editing an already-approved amendment rather than the surrounding docs, which this docs-sync pass leaves alone. A future edit to Rule 7 should say "three parsers" or name them. |
| TD-86 | S3 | role model | The shipped demo's current stage (1) has a milestone already marked done that links no work item, so the `롤모델` screen's `이 단계의 업무` reads its empty line (`연결된 업무 없음 — 로드맵에서 마일스톤에 업무를 연결해요`) on every fresh demo entry — the linked-work feature (`stageWorkOf`, 2026-09-18) is exercised only by `flow3.js`'s planted fixtures, never by the shipped demo. | Accepted (planner default) — re-linking the demo's own milestones to add a work item would move the reader's and roadmap's `업무 0/1` lines that `flow.js` already asserts; the same trade-off [TD-57](tech-debt-tracker.md) made for carry-forward. |
| TD-87 | S3 | role model | `payment_paid`'s `지금 할 것` landing (`계약 목록 ›`, `ROLE_COND_ACTIONS`) opens the `사업` tab's `deals` view with no add modal and no scroll to the specific deal or payment line the condition names — the user still has to find the right contract and payment chip by hand. | Accepted (planner default) — a payment line has no add modal of its own (it lives inside `DealModal`'s edit view on an existing contract), so there is no sheet to open the way the other eight condition types have one. |
| TD-88 | S3 | role model | `cert_held`'s `지금 할 것` landing (`도감에서 찾기 ›`) opens `CatalogModal` with the certification name prefilled in the search box, but it cannot create the certification task itself — that path is the goal's own KR ([Rule 19](../design-docs/core-beliefs.md#rule-19)) — so the button only confirms the name exists in the catalogue; the user still has to add it as a KR under a goal to actually work toward it. | By design (exec plan default 7) — creating a free-form cert task from the catalogue would need a goal to attach it to, which the screen does not have context for; recorded so a later plan does not "fix" this into a free-form task creator, which Rule 19 forbids. |
| TD-90 | S3 | E2E harness | Three of the role-screen exec plan's own assumptions did not match the source, found and corrected during implementation (phase 1 progress note): the plan's `BIZ_ADD_LABEL.deals` = `계약 추가` is **not** the deal-form title — `DealModal` reads `새 계약` and `FolioModal` reads `새 포트폴리오` (`LeadModal`/`NoticeModal` do read `리드 추가`/`공고 추가`, matching the plan) — so the `지금 할 것` landing steps assert the real titles and the `milestone_done` landing (no sheet) is asserted through the roadmap header's `마일스톤 추가` text instead; a stage's linked work row (`stageWorkOf`) renders its spans as flex items, so `innerText` carries line breaks, and the E2E taps it by whitespace-normalised text rather than `clickInModalExact`; and the footer's `세부 수정 ›` button measures 34 px tall with the plan's own `px-3 py-2` classes — one line, not a wrap, so the plan's own "≤ 32 px" ceiling for a one-line control does not hold for this specific button and the step accepts the true height instead of the plan's assumed ceiling. | Not fixed — these are corrections to the exec plan's assumptions, not defects in the shipped screen; recorded so a later plan reusing `BIZ_ADD_LABEL` for a form title, or reusing the plan's blanket "≤ 32 px" rule for every new button, does not repeat the same two mistakes. |
| TD-91 | S3 | role model | Older verdicts written before 2026-09-18 (third role-model change of the day) may still carry a `probability` field in the save — `parseRoleVerdictReply` and `importRoleVerdict` no longer write one for a new verdict, but no `migrate` block strips the field from a record that already has it, by design (Rule 12 — no `v` bump, no schema change). The field sits in the save forever, unread. | Accepted (exec plan default 5, [Rule 12](../design-docs/core-beliefs.md#rule-12)) — a future schema bump may drop it explicitly; migrating it out now would be a `migrate` edit for a field this plan deliberately treats as "unread", not "invalid". |
| TD-92 | S3 | role model / AI | `ROLE_PACKET_HEAD` rule 1 now asks the AI not to state a likelihood figure (`달성 가능성을 숫자로 쓰지 않아요.`), but the app cannot enforce this on the AI's own free text — a reply's `summary` or `basis` may still contain a percentage or a stated likelihood in prose, shown verbatim under `AI 판단 · 검증되지 않음` (the demo's own summary does exactly this: `… 도달 확률은 낮아요.`, a sentence, not a stripped figure). | By design (exec plan acceptance item 13) — stripping numbers out of AI free text the app does not parse as structured data would risk mangling a legitimate fact (a D-day, a headcount) the user asked the AI to state; the head's instruction and the `AI 판단 · 검증되지 않음` label are the only guards. |
| TD-93 | S3 | role model | Once a role model has stages, home's single row under the CV states the current stage's name (`stageName(rs)`), not the role model's own name (`role.name`) — a user with several role-model ideas over time sees only the stage name, not which role model it belongs to, once stages exist (the `단계 없음`/no-role branches do state `role.name`). | Accepted (implementer note, deviation from no plan text) — the row is one line at 390 px and the stage name is the more useful fact once stages exist; the role's own name stays reachable one tap away on the `롤모델` screen. |
| TD-94 | S3 | E2E harness | `flow8.js`'s stage-line helper asserted the quit text (`전환 조건 미충족 (0/1)`) on the headline row's `innerText`, but that text has lived on the button's `title` attribute, not its visible text, since the 2026-09-18 story-verdict change — never caught because the suite has not been run since (per the standing instruction, [TD-44](tech-debt-tracker.md)). | Fixed 2026-09-18 (third role-model change of the day, found and corrected in passing) — the helper now returns `{ text, title }` and the quit text is asserted on `title`; still not run, so this fix is itself unverified until `npm run verify`. |
| TD-95 | S3 | E2E harness | The day-job AI switch (2026-09-22) defaults `settings.workInAi` on, so every day-job meeting, work item and event a flow plants from `flow10.js`/`flow11.js` onward now enters the packets a later flow's packet-content step reads, whenever the suite is run in file order — a downstream work-packet step may see a different meeting count or order than it did while the switch defaulted off. Implementer's progress-note risk, not yet reconciled because the suite has not been run since. | Not fixed — the four form steps and the renamed `flow11.js` packet step were updated for the switch itself ([tools/e2e/README.md](../../tools/e2e/README.md)), but no step downstream of those fixtures was re-audited for a day-job record now appearing in its packet assertions; the first `npm run verify` after this change should treat any such mismatch as expected, not a regression, and update the step rather than the switch. |

## Resolved
| Id | Sev | Issue | Resolved |
|---|---|---|---|
| R-01 | S1 | `window.storage` fallback lost state on reload | 2026-09-03 — `store` → `localStorage` adapter |
| R-02 | S1 | No Vite scaffold | 2026-09-03 |
| R-03 | S1 | Saves without `v` skipped every migration block | 2026-09-07 — `v` normalised to 0 at entry |
| R-04 | S1 | `completeTask` exam branch crashed on unknown `famId`; `needsEvidence` on unknown diff | 2026-09-07 — `examOf` lookup + guards (covered by E2E) |
| R-05 | S2 | Catalog `취득 완료` never showed (exact-title match vs `…취득`) | 2026-09-07 — `certByTitle` reverse lookup |
| R-06 | S2 | Catalog list cap not shown | 2026-09-06 — cap 60 + `N종 더 있음` |
| R-07 | S2 | Evidence/study photos were write-only; image keys leaked on delete/reset | 2026-09-07 — `EvidenceViewModal`; keys removed in `removeTask`/`resetAll` |
| R-08 | S2 | Onboarding qualification list mounted all 1,011 rows | 2026-09-07 — 60-row cap, selected first |
| R-09 | S3 | Dead constants/imports/CSS/props (`DIFF_ORDER`, `certGrade`, `CLASS_NOUN`, `FRIEND_LOOK`, `MENTOR_LOOK`, `EXAM_CATS`, `DIFF_TEXT`, 11 icons, `.plumbob`, `Shell game`, `certKrDone`, `AddGoalModal exams`, `roleMatch`) | 2026-09-07 — removed; `finish-check` guards |
| R-10 | S3 | Game residue (`kind: "boss"`, `BOSS_COLORS`, 🎲, `플레이어`, "CHARACTER CREATION", "▶", `캐릭터`, `퀘스트/파트/상태창/실드`) | 2026-09-07 — schema v13/v14, terminology rename |
| R-11 | S2 | Docs claimed `askClaude`/`QuizModal`/`coachApply` existed | 2026-09-07 — removed from CLAUDE.md §4 / Rule 11; now `check-docs` + symbol index |
| R-12 | S3 | Favicon 404 | 2026-09-07 — data-URI favicon + manifest icon |

| R-13 | S1 | Extracting `roleRecommendations` out of `RoleAdviceModal` left the JSX referencing the removed local `h`, crashing the direction-advice screen | 2026-09-09 — caught by the E2E run (a page error plus a knock-on step failure), fixed by destructuring `h` from the returned gap |

| R-14 | S1 | The backup file input was inserted into `EvidenceModal` instead of the app root, so `importRef` was undefined at render and the growth tab crashed | 2026-09-09 — caught by the E2E (22 knock-on step failures from one page error), fixed by moving the input into the root render |

| R-15 | S1 | The service worker's first claim triggered `location.reload()`, so on a slow phone the app reloaded seconds after opening and discarded the user's first tap — reported as `시작하기` doing nothing in the installed app | 2026-09-09 — reload only when a controller was already in charge (a real update); reproduced at 700 kbps and covered by an E2E step asserting a first visit does not reload itself |

| R-16 | S2 | The service worker precached with a single `cache.addAll`, so one missing file — a deploy replacing assets mid-visit, say — left no cache at all and the app silently would not open offline | 2026-09-09 — the document and bundle stay all-or-nothing, icons and manifest are added individually; an E2E step now asserts the cache actually holds the document and the bundle |

| R-17 | S3 | Every build shipped screenshots nothing referenced: `gen-screenshots.js` wrote `public/screenshots/schedule.png` (and, from a Phase A probe, `calendar.png`) while `public/manifest.webmanifest` listed only three | 2026-09-11 — a shot may now carry a follow-up click, the fourth shot is the `일정` tab switched to `달력`, `calendar.png` is the fourth manifest entry and the orphaned `schedule.png` was deleted |

| R-18 | S2 | Promotion (+3 influence) and goal completion (+4 influence, +2 asset) used fixed increments, not `metricsGain` (D/10) — rule 8 documented the gap, not the rule text | 2026-09-11 — moot: the global life-metric store, `metricsGain` and both fixed increments were removed entirely (schema v19, user decision); rule 8 rewritten to describe the goal-metric-KR model that replaces them |

| R-19 | S2 | `tools/e2e/flow7.js`'s weekend-tone step excluded `today` from the day cells **before** taking its sanity count (at least four Saturdays and four Sundays in the month) — in a four-Saturday month, running the step on a Saturday undercounted to three and threw. Date-dependent, introduced 2026-09-11 with the step itself. | 2026-09-13 — the sanity count now reads the whole grid first and excludes `today` only from the tone assertions that follow it (found and fixed during the business-tab plan, unrelated to its feature work) |

| TD-02 | S2 | `TaskTab` orphan test only checked missing/deleted goal → tasks of a **done** goal appeared in no section (could not be completed or deleted). | 2026-09-13 — the unified to-do list rewrite dropped goal-scoped sections entirely: `실행` is now one time-ordered list filtered on task state only, never goal state, so a done goal's tasks render like any other row ([tasks.md](../product-specs/tasks.md), [completed plan](completed/2026-09-13-unified-todo.md)) |

| TD-39 | S2 | `tools/e2e/flow.js`'s onboarding step 4, `certification search (1,011 rows)`, called `clickText("정보처리기사")`, which matched the search `<input>` (its own value held the same text) before any result button, so no certification was ever declared at onboarding on the main E2E save — every downstream fixture silently carried zero declared certifications. | 2026-09-15 — the step now scopes the click to `button` and asserts `(1개 선택)`; the fresh-state step asserts `profile.certs` equals `["정보처리기사"]`. Found and fixed during the `cv-home` plan (home-CV rewrite), unrelated to its feature work. |

| TD-43 | S2 | `parseAssistantReply` trusted a reply-declared `kind` whenever the title named no activity, so a pasted `{"title":"견적서 송부","kind":"book"}` imported as a `독서` task; `importTasks` copied the kind through unchecked, and `flow8.js`'s paste step never asserted `events` were untouched. Found by an adversarial verifier during the 2026-09-16 meeting-workflow feasibility analysis. | 2026-09-16 — the kind comes from `detectKind(title)` only, `PACKET_HEAD` rule 3 asks for the activity in the title and the template dropped `kind`, `importTasks` re-derives and drops kind-less entries, `flow5.js` asserts the mislabelled row is refused and never imported, `flow8.js` now compares `events`. Mutation-tested: restoring the old line failed only the parser step, because `importTasks`' own check still kept the task out |
| TD-40 | S3 | `tools/e2e/ab.js`, `perf.js` and `prof.js` switched tabs by the pre-2026-09-07 label `퀘스트`, which never matched a real nav button once the tab was renamed `실행` that day. | 2026-09-16 — the `meetings-and-simplify` bench-script pass renamed every `퀘스트` reference to `할 일` (the tab's current label) in all three files alongside the `홈` → `프로필` rename, closing the label mismatch this row tracked. |
| TD-41 | S3 | `public/manifest.webmanifest`'s `tasks.png` screenshot label read `목표별 실행과 마일스톤`, describing the per-goal grouped list `TaskTab` had before its 2026-09-13 rewrite into one time-ordered list (`todoOf`); left alone by the 2026-09-15 home-CV plan, whose own screenshot step only touched the `home.png` label. | 2026-09-16 — relabelled to `기한 순으로 모은 할 일`, describing the compact one-line rows the same plan introduced; `home.png`'s label (`학력·경력·자격·시험 이력과 롤모델 근접도`) and `gen-screenshots.js`'s tab switches were updated for the renamed tabs at the same time. |
| TD-84 | S3 | The profile headline's `pct` reset to a small number at every stage boundary by design (the current stage's own fraction, floored) — a user who just completed a stage saw the headline drop from ~100 % to whatever the next stage's conditions already read, with only the stage-completion overlay marking the moment; the reader's re-assessment line's stage-rose suffix read `단계 n → n` at the *last* stage since both `pct`'s `k` and the verdict's `stageK` were capped at `n` for display. | 2026-09-18 (third role-model change of the day, [Rule 14](../design-docs/core-beliefs.md#rule-14) amendment) — `stageProgressOf` and its `pct`/`journey` figures are retired entirely, at the user's request; the headline states `단계 k/n {name} · 조건 c/m` instead, which never "resets" because it is a plain fact, not a fraction. The `n → n` cap on the last-stage delta line is unchanged in substance (`roleStageOf`'s `k` and the verdict's `stageK` are still capped at `n`) but is now a stage-count fact, not a percentage regression — see `metrics-and-role-model.md`'s [Verdict history](../design-docs/metrics-and-role-model.md#verdict-history-and-the-delta-line) section. |
| TD-89 | S3 | The journey figure (`sp.journey`) could print twice on the `롤모델` screen at once: once in the `스토리라인` section's own journey line and, when the target role also had a `사업` grade gap, once more inside that gap's `RoleStageLines` block under the collapsed `영역 등급` section. | 2026-09-18 (third role-model change of the day, [Rule 14](../design-docs/core-beliefs.md#rule-14) amendment) — the journey line and `condRatio`'s per-condition bars are deleted, not restyled; `RoleStageLines` now prints `stageLine(rs)` (the same fact line the home row's `title` carries) once, in the timeline section, and nothing inside the `사업` grade-gap block duplicates it. |
| TD-96 | S2 | Since schema v28 backfilled every existing record to the `work` track, `PACKET_TRACKS = ["biz", "personal"]` silently excluded exactly the records — day-job meetings, work items and schedule — that `오늘 업무 만들기` and `AI에게 회의 준비 묻기` on the `업무` tab exist to summarise, so the user's main use of the assistant bridge carried nothing. | 2026-09-22 — a settings switch, `settings.workInAi` (absent reads as `true`, no migration), decides: on (the default), `packetTracks(state)` returns every track, matching pre-v28 behaviour; off, the v28 exclusion applies exactly as before, with its captions and notices restored. See [SECURITY.md](../SECURITY.md#tracks--the-day-job-switch-2026-09-22), [decision-log.md](../design-docs/decision-log.md). |
| TD-97 | S2 | With the day-job-in-AI-packets switch on (TD-96), every unlinked or goal-linked work proposal still landed under `사업` (`importTrackOf`, a rule left from when a day-job record never reached the work packet at all) — a day-job proposal registered into the `업무` tab's `사업` section instead of `직장`. | 2026-09-22 — the work packet head asks for a `track` per item, `parseWorkReply` reads it, the confirm sheet shows a `직장`/`사업`/`개인` chip row per proposal preselected by `proposalTrackOf` (stated track → the resolved link's track → `직장` while the switch is on, else `사업`), and the user may repick any row before `importWork` registers it with the picked track. `importTrackOf` is removed. |
| TD-98 | S3 | notifications | Headless Chrome does not reliably surface a shown notification to `ServiceWorkerRegistration.getNotifications()`, so `tools/e2e/flow6.js` cannot assert the `확인 필요` notification actually appeared — it records the count in the step's `metrics` and asserts the `life-check` Cache API entry (the app's own rebuildable mirror of the rendered text) instead. | Not fixed — a headless-Chrome limitation, not an app defect; a real-device check is the only way to observe an OS notification firing, tracked alongside backlog item 2. |
| TD-99 | S3 | notifications | Chrome Periodic Background Sync (the `확인 필요` notification's background refresh, installed app only) has not been verified on a real device — CDP can dispatch a `periodicsync` event for the E2E, but Chrome's own decision of *when* to actually fire one in the wild (an engagement heuristic, floor ≈ 12 h) cannot be simulated or observed by Puppeteer. | Not fixed — same class of gap as the calendar-file `VALARM`/re-import checks (backlog item 2); record the result here, dated, once it runs on a device. |
| TD-100 | S3 | notifications | The service worker's `CHECK_*` literals (`tools/harness/gen-sw.js`) duplicate the app's own `CHECK_*` constants by necessity — the worker is a plain string template and cannot import from `src/LifeManager.jsx` — so the two sets can drift silently if one is edited without the other. | Accepted — smoke section 7 pins the two sets to agree (`swSource(...)` is asserted to contain the same cache/tag/request literals the app defines); a future refactor should keep that pin rather than relax it. |
| TD-101 | S3 | issue list | A goal task has no `track` field of its own (tasks are never tracked — [Rule 18](../design-docs/core-beliefs.md#rule-18) scopes a task to a goal, not a business line), so `issueListOf`'s `할 일` section lists every open goal task under `개인` regardless of the goal's own area or subject matter — a day-job-relevant goal task still surfaces under the private-life group. | Accepted (implementer note) — inventing a track for tasks would be a scope change to the goal engine this plan does not make; the issue list states the fact the data model already has. |
| TD-102 | S3 | notifications | The since-mode work packet can be **larger**, not smaller, than the full packet on the same heavy save: since-mode adds two sections the full packet never has (`이전 회의록의 새 기록`, `문서 ({since} 이후)`), so a save with many older meetings carrying fresh progress/follow-ups or many recently-added documents can push the since-mode packet closer to or past `WORK_PACKET_MAX` (20,000) faster than the full packet would, even though since-mode's own `최근 회의록` list is shorter. | Accepted (implementer note) — the trim order already drops both new sections before the meeting floor, so the cap is never exceeded; the effect is a caption-visible fact (`WorkBridgeModal`'s reductions still fire in order), not a correctness bug. |
| TD-103 | S3 | meetings | `buildReviewPacket`'s `fuLines` and `weekFacts` (the weekly review's per-track facts) both read `m.followUps` directly, not through `followUpsOf` — a [training record](../product-specs/meetings.md) (`kind: "training"`) saved (or switched to `교육`) before 2026-09-22 that still carries stored follow-ups has them counted in the `주간 회고` packet and the `주간 리뷰` sheet's fact lines, even though every other reader now treats the record as reference only. | Accepted (implementer note, literal reading of the reference-only change) — neither the review packet nor the weekly facts is a to-do surface the user acts on directly, and a training record now always writes `followUps: []` going forward, so the gap only ever concerns pre-existing data; a fix is a one-line `followUpsOf` swap in both call sites, left for a future pass. |
| TD-104 | S3 | meetings | The daily reader's `{since} 이후 새로 들어온 것` section lists `m.progress` entries directly, not gated by `isTraining` — a training record's stored progress entries (if any, from before 2026-09-22) still appear in this section, even though the reader's `오늘·내일 회의 준비` and `기한 지남 후속` sections now exclude a training record's stored data entirely. | Accepted (implementer note, literal reading of the reference-only change) — the section states what "arrived" since the last visit, and a stored progress entry did arrive; a training record now always writes `progress: []` going forward, so the gap only ever concerns pre-existing data. |
| TD-105 | S3 | meetings | `meetingsOfTask` (the task sheet's `관련 회의록` reverse list) still names a training record whose stored `taskIds` include the task — it is not gated by `isTraining`, even though the meeting form and view now hide a training record's own task-link picker and `연결된 할 일` block entirely. | Accepted (implementer note, literal reading of the reference-only change) — the reverse list lives on the task sheet, not on the meeting, and a training record now always writes `taskIds: []` going forward, so the gap only ever concerns a link stored before 2026-09-22. |
| TD-106 | S2 | daily gate | **No escape hatch, by the user's own decision.** The [daily gate](../product-specs/daily-gate.md) has no close, no skip and no settings access while it stands — a day with no access to an external AI chat (Claude, ChatGPT, or similar) cannot be passed, since the quiz step needs a pasted reply from one. | Accepted — user decision 2, 2026-09-24; stated on the gate's own screen and in [RELIABILITY.md](../RELIABILITY.md#known-limits). |
| TD-107 | S2 | daily gate | A bug in `buildQuizPacket`/`parseQuizReply`, or a model that never returns five valid questions, locks the user out of the **entire app** — not just the gate — because there is no path around it and `SettingsModal` (and therefore backup export) is itself unreachable while the gate stands. | Accepted — same decision as TD-106; the only recovery is a new deploy that fixes the bug, or a hand edit of the `liferpg-state-v1` `localStorage` entry to plant a `passedAt` stamp. |
| TD-108 | S3 | daily gate | `백업 내보내기`/`백업 불러오기`/`데이터 초기화` (`SettingsModal`) are unreachable while the gate stands — `settings` is not one of `GATE_MODAL_TYPES` — so a user mid-lockout (TD-106/TD-107) cannot even fall back to a backup file from inside the app. | Accepted — a direct consequence of the no-escape decision, not a separate defect; noted in [SECURITY.md](../SECURITY.md#the-backup-file) and [daily-gate.md](../product-specs/daily-gate.md#the-settings-line). |
| TD-109 | S3 | daily gate | `quizHeld` (the parsed quiz items behind `같은 문제 다시 풀기`) lives only in the root's component state, never in `state` — a reload of the app between a failed attempt and a retry loses it, forcing a fresh AI round trip even though the same questions could otherwise be reused. | Accepted (planner decision 7) — the gate states the fact on screen (`앱을 다시 열면 문제가 지워져요 — 새 퀴즈를 요청해요.`) whenever it applies, rather than hiding the gap. |
| TD-110 | S3 | daily gate, daily reader | Interacts with TD-62 (above): inside the gate, `다 읽었어요` stamps `act.briefingSeen` exactly like `closeBriefing` does — so a re-read forced by a failed quiz (which clears both read stamps) can trigger `readerSince`'s "already closed once today" fallback (yesterday) a second time the same day, the same gap TD-62 already accepted outside the gate. | Accepted — the same known limitation, now also reachable through the gate's fail/retry cycle; no new behaviour, no new fix needed beyond TD-62's own note. |
| TD-111 | S3 | e2e | `closeModal` (`tools/e2e/run.js`) cannot close the daily gate (no X, an inert backdrop) and must never be called while the gate is active; `plantGate()` writes a **synthetic** `passedAt: "00:00"` stamp directly into the save, never a time the app itself wrote. | Accepted — documented on both helpers and in [tools/e2e/README.md](../../tools/e2e/README.md) and [daily-gate.md](../product-specs/daily-gate.md#e2e-coverage-written-not-run--standing-user-instruction); every flow that needs the gate out of the way uses `plantGate`/`passGate`/`reload({ keepGate: true })` instead. |
| TD-112 | S3 | daily gate | `useReadEnd`'s scroll-listener fallback (used only when `IntersectionObserver` is unavailable) observes nothing when the end sentinel has no `.overflow-y-auto` ancestor between it and the scrolling element — `seen` then never becomes `true` and `다 읽었어요` stays disabled forever in that case. | Accepted (implementer note, Phase 1 deviation 7) — the `IntersectionObserver` path is what actually runs in Chrome, so this fallback is unexercised in practice; a future browser without the API would need a real fix. |
| TD-113 | S3 | e2e | `flow4.js`'s pre-v11 migration fixtures (a v10 save, a save with no `v` field) reload with `keepModal: true` and call `h.passGate()` by hand, because the `v < 11` migration block rebuilds `act` from its own four fields and drops whatever stamp a plain `reload()` would otherwise have planted first. | Accepted — a plan error found during implementation: G7 assumed the v15 block's `...(s.act \|\| {})` spread kept a planted stamp, which holds only for v15 and later blocks, not v11's full rebuild. |
| TD-114 | S3 | daily gate, smoke | Mutation 5 (`recordQuiz` not clearing the read stamps on a fail) is not catchable by `tools/harness/smoke-logic.js`: `recordQuiz` is a root component closure, not a module-level pure function smoke can lift. | Accepted — a plan error found during implementation; only the throwaway UI run and `flow12.js` step 6 (both written, not run) catch this mutation. |
| TD-115 | S3 | push | The app has **no `pushsubscriptionchange` handler** — when Chrome rotates or drops a push subscription, nothing on the phone notices; `tools/push/send.mjs` simply starts exiting 2 on the next scheduled run, and the user learns of it only from Actions, from a workflow-failure e-mail (TD-119), or from the wake-up's own silence. | Accepted (planner decision, Open risks) — the user re-copies the subscription from `설정 › 푸시 알림`; a handler that re-subscribes automatically and re-uploads a secret would need a server-side token the app does not have, out of scope for this plan. |
| TD-116 | S3 | push | GitHub **disables a scheduled workflow after 60 days with no commit activity** on the repository — `notify.yml`'s cron then simply stops firing, with no in-app or Actions-log signal the user would see without visiting the repository. | Accepted — a `workflow_dispatch` run (`Actions › Daily push › Run workflow`) re-enables it; stated in [RELIABILITY.md](../RELIABILITY.md#known-limits). |
| TD-117 | S3 | routine, daily gate | `act.opened[date]` **cannot tell a routine open from a manual one** — a phone routine that opens the app while locked stamps the moment the app painted, exactly as a deliberate open would. | Accepted (planner decision 4) — stated as a fact in the `자동 실행` settings caption itself, not hidden; the stamp was never meant to distinguish the two, only to prove *an* open happened. |
| TD-118 | S3 | push | **A push tap in a browser tab (not an installed app) opens a Chrome tab, not the app** — the settings sheet's capability caption states this (`설치된 앱이 아니에요 — 알림을 누르면 Chrome 탭으로 열려요`) rather than hiding the difference, but the app cannot itself force the installed-app landing on a tap. | Accepted — the same class of gap the `확인 필요` notification and Periodic Background Sync already carry (installed-app-only capability, stated in a caption); installing the WebAPK is the only fix, and is the user's own choice. |
| TD-119 | S3 | push | A malformed `PUSH_SUBSCRIPTION` or an expired subscription makes `send.mjs` exit non-zero, which triggers **GitHub's own default workflow-failure e-mail** to the repository owner — an out-of-band channel this plan did not design but that becomes the de facto failure signal, since the app itself has no `pushsubscriptionchange` handler (TD-115). | Accepted — a side effect of GitHub Actions' own defaults, not a feature; a user who wants to suppress it can turn off "Actions" failure notifications in their own GitHub account settings, outside this repository's control. |
| TD-120 | S3 | e2e | **Fixed in this plan, recorded for the record.** `tools/e2e/flow4.js`'s v18 → v19 exact `act` key-set assertion (`"briefingSeen,lastActive,lastReview,shieldMonth,shieldsLeft,streak"`) was already false before this plan — `plantGate()` (added 2026-09-24) writes `gate` into `act` before every reload the fixture takes — and the new `opened` stamp (2026-09-25) would have broken it a second time; the never-run suite hid both. | Fixed (Phase 1, A8) — the assertion now filters `gate` and `opened` out of the key list with a comment naming why; every other `act` assertion in the suite reads named fields or diffs within one step with no reload between reads, so nothing else was affected. |
| TD-121 | S3 | plan, push | **Plan error, corrected during implementation.** P2 as written would have wired `onToast={showToast}` on `SettingsModal`; `showToast` takes a `{ msg }` object, while `copyPacket`'s `onToast` parameter calls its argument as a plain string, so the literal wiring would have rendered an empty toast pill on every copy. | Fixed (Phase 2 deviation 1) — the root instead passes `onToast={(msg) => showToast({ msg })}`, the file's existing convention for adapting a string-taking callback to `showToast`'s shape; no duplicate toast-building logic was added. |

## Allowlist mirror (`tools/harness/finish-allowlist.json`)
Entries here are two-phase artefacts, not accepted debt: `finish-check` counts references corpus-wide, so a constant that only the next phase renders is flagged, and the alternative — a fake reference — is worse. The previous set (`EVENT_SOON_DAYS`, schedule Phase A) was removed on 2026-09-11, when Phase C wired it into `buildBriefing` and the home briefing card.

Open: none — the allowlist is empty.

Closed on 2026-09-12 by Phase B, which rendered all six: `wonText` (`BizTab` header lines and row totals), `RATE_UNIT` (`단가` view and `RateModal` chips), `BIZ_REVENUE_MONTHS` (`계약` view, `최근 6개월`), `IMG_FILE_MAX` / `resizeImageFit` / `saveImageChecked` (`FolioModal` attachment and submit).

Closed on 2026-09-13 by Phase C, which read both in the `biz` briefing section: `QUOTE_STALE_DAYS` (the stale-quote line) and `DEAL_END_SOON` (the contract-ending-soon line).
