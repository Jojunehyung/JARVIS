# Product specs — index
<!-- src: SPEC-3 -->

Per-screen behaviour as implemented. Rules by number → [../design-docs/core-beliefs.md](../design-docs/core-beliefs.md); visual tokens → [../DESIGN.md](../DESIGN.md).

## Screen map
Bottom tab bar (`NAV`, seven tabs since 2026-09-17, `grid-cols-7`): **프로필** `HomeTab` · **목표** `GoalsTab` · **할 일** `TaskTab` · **업무** `WorkTab` · **일정** `ScheduleTab` · **미팅** `MeetingsTab` · **사업** `BizTab`. Internal tab keys are unchanged (`home`, `tasks`, …) — only the nav labels and, for `home` and `tasks`, the everyday names in this doc set changed. Header on every tab: `LIFE MANAGER` / nickname · status / 🔥 streak · 🛡 shields. Phases: `loading` → `onboard` (`Onboarding`, six steps + title) → `main`. The former sixth tab, `성장`, was removed 2026-09-15 — home is now one CV card plus a role-model proximity line; see [home.md](home.md) and [growth.md](growth.md) (retired).

Modals (`Modal` shell, one at a time via `modal` state, 39 values — `roleAdvice` retired and `roleEdit` added 2026-09-18, second change of the day, so the count stays 39, up from 38 at schema v28): `BriefingModal`, `JournalModal`, `BridgeModal`, `ReviewModal`, `AddGoalModal`, `AddTaskModal`, `EvidenceModal`, `EvidenceViewModal`, `StudyVerifyModal`, `ActivityLogModal`, `PromoteModal`, `RoleModal` (2026-09-18, the `롤모델` screen, replacing `RoleModelModal`'s and `RoleAdviceModal`'s user-facing roles), `RoleModelModal` (now the `roleEdit` sub-screen, title `롤모델 세부 수정`, reached through `RoleModal`'s `세부 수정 ›`), `CatalogModal`, `EventModal`, `CalendarExportModal`, `DealModal`, `RateModal`, `FolioModal`, `ProfileModal`, `SettingsModal`, `AchievementWallModal`, `TaskDetailModal`, `EventDetailModal`, `BizTodoModal`, `ProjectModal`, `MeetingModal`, `MeetingViewModal`, `DocumentModal`, `WorkModal`, `WorkBridgeModal`, `PrepBridgeModal`, `DailyReaderModal`, `MilestoneModal`, `TimeLogModal`, `LeadModal`, `NoticeModal` (v28's five new values also include `WorkBridgeModal` rendered a second time as `reviewBridge`, so the tally is `milestone`/`timeLog`/`lead`/`notice`/`reviewBridge`), `RoleVerdictModal` (2026-09-18, `roleVerdict` — the fifth bridge packet's send/paste/confirm sheet, opened from `RoleModal`'s `AI에게 판정 묻기 ›` button). Full-screen overlays (`Overlay`): `gradeup` (RANK UP), `achieve` (ACHIEVEMENT), `stage` (STAGE, 2026-09-18). One toast (`ToastHost`).

| Spec | Screen / flow |
|---|---|
| [scenario-harness-engineer.md](scenario-harness-engineer.md) | Reference scenario the demo data reproduces |
| [new-user-onboarding.md](new-user-onboarding.md) | Title → 6 steps → computed starting grades |
| [install-and-backup.md](install-and-backup.md) | Installing the app on Android, offline behaviour, and the backup file |
| [daily-briefing.md](daily-briefing.md) | The daily briefing (no longer auto-opens since schema v27 — see [daily-reader.md](daily-reader.md)) reached from the `할 일` header or the daily reader, and the journal; per-track prefixes/counts and `사업`'s payment/lead/notice/milestone lines since v28 |
| [daily-reader.md](daily-reader.md) | `오늘 읽을 것` (schema v27) — a full-content modal, derived at render, that opens once a day in the briefing's former place and from two buttons; ten sections (nine since v28, `사업 로드맵`/`사업 파이프라인 · 공고` added; a tenth, `롤모델 판정`, since 2026-09-18) plus a `브리핑 ›` link; track heads on five sections, `직장` first; no tick, no confirm, nothing stored beyond the existing `act.briefingSeen` stamp |
| [home.md](home.md) | The CV card (identity, records, area grades → `PromoteModal`, a `오늘 읽을 것 ›` button since schema v27), a stage-progress headline (2026-09-18, replacing v28's role-stage line) above the role-model proximity line → the `롤모델` screen (`RoleModal`, 2026-09-18, second change of the day, replacing `RoleAdviceModal` — story templates, the verdict CTA and `판정 기록`, the `스토리라인` timeline with `지금 할 것`, the collapsed `영역 등급`, `세부 수정 ›`), `SettingsModal` (role model, backup, reset), `AchievementWallModal`, `ProfileModal` (photo, personal facts, education/career records) |
| [goals.md](goals.md) | OKR cards, four KR types, KR check-in, goal status, `AddGoalModal` — the only screen showing goal progress and pace |
| [tasks.md](tasks.md) | One time-ordered list (`todoOf`) shown as compact `TodoRow`s (lead chip, title, at most one marker); tapping a row opens `TaskDetailModal` / `EventDetailModal` (gained an `EventChecks` block for a project-linked event, schema v27) / `BizTodoModal`, and completion happens only inside `TaskDetailModal`; the `할 일` / `완료` views, the `브리핑 열기 ›` header button, `AddTaskModal` with the KR bridge, `CatalogModal` |
| [daily-work.md](daily-work.md) | `업무` tab (schema v25; derived carry-forward, a meeting-prep card and a third `source: "meeting"` since v26; documents, a pre-meeting checklist and `AI에게 회의 준비 묻기` since v27; a track, a weekly time budget/log and a generalised `WorkBridgeModal` since v28): dated work items typed by hand, proposed by the assistant bridge and confirmed per item, or mirrored from a meeting follow-up — `MeetingPrepCard` (a `div` since v27, TD-64), a day pager grouped by track since v28 whose today view derives carried undone items (`이월 {n}일`), `WorkModal` (a track row and minutes field, v28), `WorkBridgeModal` (`오늘 업무 만들기`, generalised v28 for the `주간 회고` packet too), `TimeLogModal` (v28), `PrepBridgeModal` (v27), the meeting progress log and `AI에 보내지 않기` flag that feed it, what a work item never does |
| [growth.md](growth.md) | Retired 2026-09-15 — a redirect table to where each former block now lives |
| [schedule.md](schedule.md) | `일정` tab: calendar-only since 2026-09-16 — the month grid and its selected-day panel, `EventModal` (optional `projectId` picker since schema v26; a track row since v28), an optional pre-meeting checklist (`events[].checks`, `EventChecks`, schema v27), the `캘린더로 내보내기` phone-calendar export (`CalendarExportModal`, per-source table extended with five v28 kinds), what an event never does |
| [meetings.md](meetings.md) | `미팅` tab (schema v23; progress log and `AI에 보내지 않기` schema v25; structured follow-up items mirrored to work items, and meeting-prep rows, schema v26; documents and `확인할 것 가져오기` schema v27; a track on every project/document/event/work item/deal, derived for a meeting, since v28): project-grouped, hand-written meeting minutes — `ProjectModal`, `MeetingModal`, `MeetingViewModal`, `DocumentModal` (v27), the caps and storage-budget guard, the optional link to a `일정` event, the day-job demo project (v28), what a meeting never does |
| [documents.md](documents.md) | Top-level `documents[]` (schema v27; a track since v28) — a record of what a file says, on a meeting project or none; no file is ever stored; `DocumentModal`, the `미팅` tab listing, storage arithmetic, what a document never does |
| [business.md](business.md) | `사업` tab, six views since v28: contracts (`계약`), unit prices (`단가`), the portfolio (`포트폴리오`), a roadmap of milestones (`로드맵`), a hospital sales pipeline (`리드`) and national-project notices (`공고`); payment chips and lump-sum payment lines, the revenue roll-up, `DealModal` / `RateModal` / `FolioModal` / `MilestoneModal` / `LeadModal` / `NoticeModal`, the image and storage guards, what a business record never does |
| [evidence-modals.md](evidence-modals.md) | `EvidenceModal`, `EvidenceViewModal`, `ActivityLogModal`, `StudyVerifyModal` |
| [feedback-overlays.md](feedback-overlays.md) | RANK UP / ACHIEVEMENT / STAGE (2026-09-18) overlays and toasts |
