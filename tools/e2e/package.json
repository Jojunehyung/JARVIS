{
  "name": "life-manager-e2e",
  "private": true,
  "description": "Life Manager E2E harness — drives the installed Chrome/Edge through puppeteer-core (no browser download)",
  "scripts": {
    "test": "node run.js --tag run",
    "perf": "node perf.js --tag run",
    "prof": "node prof.js"
  },
  "dependencies": {
    "puppeteer-core": "^24.0.0",
    "source-map": "^0.7.4"
  }
}
