# ARCHITECTURE.md — Life Manager
<!-- src: CL-2 --><!-- src: CL-3 --><!-- src: CL-4 --><!-- src: SPEC-10 -->

Single-page React app in one file. This document is the map: where things are, how state flows, how it is built and verified. Rules live in [docs/design-docs/core-beliefs.md](docs/design-docs/core-beliefs.md); the generated symbol index is [docs/generated/symbol-index.md](docs/generated/symbol-index.md).

## Stack
Vite 5 · React 18 · Tailwind v3 (core utilities only) · lucide-react. Node 24 at `C:\Program Files\nodejs` (add it to PATH in Git Bash: `export PATH="/c/Program Files/nodejs:$PATH"`). No backend, no network at runtime.

## Files
```
src/LifeManager.jsx    the app (≈ 12,700 lines) — see "File regions"
src/main.jsx           React root
src/index.css          Tailwind directives
index.html             shell; data-URI favicon; manifest link
public/                icon.svg, manifest.webmanifest
vite.config.js         dev/prod build → dist/
vite.demo.config.js    single-file demo build → release/life-demo.html (IIFE script inlined before </body>; opens from file://)
tools/e2e/             puppeteer-core E2E harness (see docs/RELIABILITY.md)
tools/harness/         finish gate, verify, docs generation (see AGENTS.md §8)
tools/harness/gen-sw.js  the service worker template (swSource) — hashed-asset precache, the `확인 필요`
                          notification's `notificationclick`/`periodicsync` handlers (2026-09-22, docs/product-specs/notifications.md), never a reload
docs/                  product, design, engine, plans, generated tables
```

## File regions (`src/LifeManager.jsx`, banner comments `/* ── … ── */`)
| Region | What lives there | Grep anchor |
|---|---|---|
| Constants & rules | `RANKS`, `DIFFS`, `EVIDENCE_MIN`, `certP`, `achGrade`, `GRADE_*`, `CERT_CATS`, **`CERTS` (1,011 rows)**, `DIFF_RAW_VERSION` | `const CERTS = [` |
| Onboarding option data | `AGE_OPTS … OUTPUT_OPTS`, `MAJOR_FIELDS`, `KNOWLEDGE_FIELDS`, `gFromD`, `computeGrades`, the CV chip lists `EDU_LEVELS` / `EDU_STATUS` / `EMP_KINDS` (deliberately not in `DATA_TABLES`), the CV → starting-grade helpers `ageOf`, `ageText`, `careerMonths`, `careerText`, `eduKeyOf`, `careerKeyOf`, `displayName`, `topEdu`, `latestCareer`, the home-CV helpers `cvSummaryOf` (shared with `buildAssistantPacket`'s `## 이력` line) and `heldCertsOf` (declared + earned certifications, shared with `ProfileModal`'s `보유 기록`), `GATE_CHIPS`, `AREA_PRESETS`, `TASK_TEMPLATES`, `detectKind`, `goalKinds` (`ROLE_PRESETS` removed 2026-09-18 — its only readers, `RoleModelModal`'s chip row and a `gen-cert-stats.js` name list, are both gone) | `초기 설정 선택지` |
| Storage adapter | `KEY`, `store` (`get/set/del`, JSON, memory fallback) — **call sites are frozen** — plus the image-storage guards `persisted`, `storageUsedBytes`, `storageUsedWith` (`state`-aware, one render ahead of `storageUsedBytes()` alone — read by the meetings storage guard and its counts line), `saveImageChecked` and their constants `IMG_FILE_MAX`, `THUMB_MAX_EDGE`, `THUMB_MAX_CHARS`, `STORAGE_BUDGET` | `const store =` |
| Illustrations & utils | `PortraitSprite`, `Portrait`, `resizeImage`, `resizeImageFit` (aspect-preserving sibling resize, never upscales), `TrophySvg`, `TIER_COLORS`, empty-state SVGs, `uid`, `dstr`, `shiftDay`, `monthStr`, `Bar`, `Chip`, `DiffBadge`, `CertBadge`, `Modal` | `function TrophySvg` |
| Exam engine | `EXAMS` (17 families), `EXAM_BY_ID`, `examOf`, `LANG_KO`, `DIM_STEPS`, `calcExamPayout`, `POINT_POLICY_VERSION`, plus the display-only exam score helpers `EXAM_LABEL_NUM`, `EXAM_SCORE_MAX_LEN`, `examScoreNumeric`, `examScoreError`, `examScoreBetter`, `examBestText` (2026-09-16, schema v22 — read/write payout unchanged, [scoring-engine.md](docs/design-docs/scoring-engine.md)) | `function calcExamPayout` |
| Goal engine | `needsEvidence`, `ddayStr`, `krProgress`, `krDoneCount`, `goalProgress`, `elapsedRatio`, `paceOf`, `krRemainText`, `roleAreas`, `certGainOf`, `examBandGain` | `const paceOf =` |
| Job-fit weighting | `DIR_CATS`, `JOB_FIELDS`, `areaCatHints`, `TIER_MULT`, `TIER_CLS`, `WEIGHT_MATRIX`, `CERT_W_EXC`, lookup indexes (`CERT_BY_NAME`, `CERTS_LONGEST_FIRST`, `CERT_NAME_LC`, `CERTS_BY_CAT`, `certOf`, `certByTitle`), `DIR_ALIAS`, `normDirs`, `jobWeightForCert` | `const WEIGHT_MATRIX = {` |
| Business | Records, never tasks: at the head (v28), `TRACKS`/`TRACK_LABEL`/`TRACK_TONE`/`TRACK_OPTIONS`/`PACKET_TRACKS_NO_WORK`/`workInAiOf`/`packetTracks`/`trackOf`/`byTrack` — the track vocabulary every record kind and packet reads, and (2026-09-22) the day-job-in-AI-packets settings switch every packet reads through `packetTracks(state)` (on by default — `settings.workInAi`, [docs/design-docs/assistant-bridge.md](docs/design-docs/assistant-bridge.md#tracks--what-leaves-the-device-and-the-day-job-switch-v28-the-switch-2026-09-22)). Then `wonText`, `monthAdd`, `monthsBetween`, `dealMonths`, `dealEnd`, `dealTotal`, `dealCostTotal`, `marginOf`, `dealPhase`, `monthRevenue`, `dealBacklog`, `billedMonths`, `revenueByMonth`, `bizSummary` — the one object the tab header, the briefing, the `할 일` header's business button and the packet all read, now also carrying `payDue`/`payOverdue`/`payPaidMonth` (v28) — plus `RATE_UNIT`, `DEAL_STATUS`, `DEAL_PHASE_LABEL` and the constants `BIZ_REVENUE_MONTHS`, `QUOTE_STALE_DAYS`, `DEAL_END_SOON`, `DEAL_MAX_MONTHS`, `BIZ_ALERT_MAX`. **Payment lines (v28)**: `PAYMENT_KIND`, `PAYMENT_SOON_DAYS`, `PAYMENT_ALERT_MAX`, `DEAL_PAYMENTS_MAX`, `dealPayments`. **Roadmap (v28)**: `MILESTONE_LIMITS`, `MILESTONE_LINKS`, `MILESTONE_STATUS`, `ROADMAP_STAGES`, `ROADMAP_ALERT_MAX`, `MILESTONE_WORK_ROWS`, `ROADMAP_SEED`, `seedMilestones`, `milestoneOrder`, `milestoneWork`, `elapsedBetween`, `milestonePace`, `stageOrderNote`, `milestoneLine`, `milestoneTrack`. **Pipeline and notices (v28)**: `LEAD_STAGE_LABEL`, `LEAD_LIMITS`, `LEAD_SOON_DAYS`, `NOTICE_STATUS_LABEL`, `NOTICE_LIMITS`, `NOTICE_DOCS_MAX`, `NOTICE_SOON_DAYS`, `NOTICE_ALERT_MAX`, `leadOrder`, `leadOverdue`, `noticeOpen`, `noticeSoon`, `noticeOrder`, `leadLine`, `noticeLine` | `const BIZ_REVENUE_MONTHS =` |
| Daily assistant | `daysBetween`, `mondayOf`, `agendaOf`, the event helpers (`occurrencesOf`, `eventsOn`, `upcomingEvents` + `EVENT_*` constants), `TAB_ACTIONS` (the tab-switch whitelist `closeBriefing` checks; `["home", "goals", "work", "schedule", "meetings", "biz"]` since 2026-09-17 — `"growth"` retired 2026-09-15, `"work"`/`"meetings"` added for the carried-work and overdue-follow-up briefing lines), `buildBriefing` (returns `{ sections }` only; gained a `회의 준비` section and a carried-work line, both schema v26, reading `meetingPrepOf`/`state.work`; the `today`/`events`/`prep` items gain per-track counts and the `biz` section gains payment/lead/notice/milestone lines, v28), `PACKET_HEAD`, `PACKET_BIZ_LINES`, `packetSection` / `bizPacketLines` (module-level, shared by every packet since 2026-09-17; filtered to `packetTracks(state)` deals v28), `buildAssistantPacket` (`briefLines` filters by `packetTracks(state)` and drops `packet: false` items, v28), `parseAssistantReply`, the second bridge packet — `WORK_PACKET_*` constants (gained `WORK_PACKET_FOLLOWUPS` v26), `WORK_PACKET_HEAD`, `oneLineText` (lifted out of `buildWorkPacket` to module level v26, shared with `MeetingPrepCard`), `meetingPacketLines` (v27, `buildWorkPacket`'s local `meetingLines` lifted to module level with an `openOnly` knob so `buildPrepPacket` reuses it; the work-packet output stays byte-identical), `buildWorkPacket` (gained per-meeting follow-up lines and a `업무 기록 (이월·어제·오늘)` records section, v26; filters meetings/events by `packetTracks(state)`, v28; gained since-mode — `{ since }`, `act.workRefreshedAt` — 2026-09-22, [assistant-bridge.md](docs/design-docs/assistant-bridge.md#since-mode--the-incremental-work-packet-2026-09-22); 2026-09-22, same-day: filters `!isTraining(m)` too, in both full and since-mode, so a training record never reaches `오늘 업무 만들기`), `workSinceOf` (same-day filter), `workPacketHead`, `WORK_PACKET_DOCS`/`_DOC_CLIP`/`_OLDER_PROGRESS`/`_OLDER_FOLLOWUPS` (2026-09-22), `parseWorkReply` (each proposal gains a parsed `track`, 2026-09-22), `replyTrackOf` (a proposal's stated track label or internal value, else `null`, 2026-09-22), `linkTrackOf` (a resolved link's project/meeting track, else `null`, 2026-09-22, the root's old `importTrackOf` lifted to module level), `proposalTrackOf` (the confirm row's preselected track: stated → link's → the day job while `workInAiOf` else the business, 2026-09-22), `WORK_LINK_KINDS` (new 2026-09-17, [docs/design-docs/assistant-bridge.md](docs/design-docs/assistant-bridge.md)) — the third bridge packet (v27, the Rule 7 amendment "second of the day") — `PREP_PACKET_*` constants, `PREP_PACKET_HEAD`, `buildPrepPacket` (one project event's minutes, documents, linked tasks and same-client contracts; own 20,000-char cap, ten-step trim; filters by `packetTracks(state)` and refuses a `work`-track event only while the day-job switch is off, v28), `parsePrepReply` (reads only `checks`/`note`; folds a proposal's `basis` into its text when it fits) — the daily reader (v27) — `READER_*` constants, `readerSince` (three cases off `act.briefingSeen`, [TD-62](docs/exec-plans/tech-debt-tracker.md)), `buildReader` (nine sections since v28 — `roadmap` and `pipeline` added — full content, reuses `buildBriefing`'s `biz`/`goals` items, `withHeads` inserts track heads into five sections; gained a `이슈 목록 ›` footer button and the `decisions` row's `kindPrefix`, 2026-09-22) — the `확인 필요` notification (2026-09-22, no schema bump) — `CHECK_MINUTES_DAYS`/`CHECK_LIST_MAX`/`CHECK_TAG`/`CHECK_CACHE`/`CHECK_CACHE_REQ`/`CHECK_STALE_MS`/`CHECK_SYNC_MIN_MS`/`CHECK_DEBOUNCE_MS`, `checkSummaryOf`, `checkNotificationOf` (both pure, [notifications.md](docs/product-specs/notifications.md)), `checkNotifyOf` (beside `workInAiOf`), `OPEN_PARAM_TYPES` (the `?open=` boot param and the service-worker message's allowed `modal.type` values) — the issue list (2026-09-22) — `ISSUE_WORK_ROWS`/`ISSUE_EVENT_DAYS`/`ISSUE_EVENT_ROWS`/`ISSUE_TRAINING_ROWS`/`ISSUE_PREV_MEETINGS`/`ISSUE_FOLLOWUPS`/`ISSUE_DECISION_CLIP`/`ISSUE_SUMMARY_CLIP`/`ISSUE_LEARNED_CLIP`/`ISSUE_PROJECT_TRACKS`, `issueListOf` (pure, [issue-list.md](docs/product-specs/issue-list.md)) — `todoOf` (the single time-ordered expansion of tasks + events + business rows, read only by the `할 일` tab since home stopped showing any date-scoped fact) with `TODO_GROUPS`, `TODO_KIND_RANK`, `todoKey`, `todoSort`, `monthEndDate`. **Role stages (v28)**: `ROLE_STAGES_MAX`, `STAGE_CONDS_MAX`, `ROLE_STAGE_NAME`, `LEAD_STAGES`, `NOTICE_STAGES`, `COND_TYPES`, `ROLE_STAGE_SEED`, `seedStages`, `condValue`, `condText`, `roleStageOf`, `quitText`, `stageLine`, `stageName` (2026-09-18, third role-model change of the day, beside `stageLine`). **Weekly time budget and review (v28)**: `weekMinutes`, `weekDaysLeft`, `hoursText`, `bizHoursOf`, `timeLine`, `weekFacts`, `weekFactRows`, `REVIEW_PACKET_*` constants, `REVIEW_PACKET_HEAD`, `buildReviewPacket` — the fourth bridge packet. **Role story and verdicts (2026-09-18)**: `ROLE_STORY_MAX`, `ROLE_VERDICTS_MAX`, `ROLE_VERDICT_DAYS`, `ROLE_VERDICT_SUMMARY`/`_BASIS`/`_POSITION`/`_GAPS`/`_GAP`, `ROLE_STAGE_WHY`, `lastVerdictOf`, `roleVerdictDue` — the reader's tenth section (`role`, `롤모델 판정`) is added in `buildReader` here; `ROLE_PACKET_*` constants, `ROLE_PACKET_HEAD`, `condTypeLines`, `buildRoleVerdictPacket` (the fifth bridge packet), `parseRoleVerdictReply` (`probText`/`condRatio`/`stageProgressOf` and the packet's/parser's `probability` handling all retired 2026-09-18, third role-model change of the day, the [Rule 14](docs/design-docs/core-beliefs.md#rule-14) amendment). **The role screen (2026-09-18, second change of the day)**: `ROLE_STORY_TEMPLATES` (three tap-to-fill story templates), `ROLE_COND_ACTIONS` (the one landing per `COND_TYPES` value), `storyWithTemplate`, `stageWorkOf` (a stage's undone linked work items, reading `workOn` below) | `const agendaOf =` |
| Calendar export | The `.ics` phone-calendar file (RFC 5545): ten `ICS_*` constants (`ICS_MILESTONE_LEAD_DAYS`, `ICS_CHECK_LEAD_DAYS` added v28), the `ics*` helpers (`icsText`, `icsFold`, `icsDate`, `icsLocal`, `icsAddMinutes`, `icsUtcStamp`, `icsDuration`, `icsUid`), `calendarExportOf` (selects and shapes entries from `events`/`tasks`/`goals` and, since v28, `meetings[].followUps`/`events[].checks`/`milestones`/`deals[].payments`/`notices`), `buildIcs` (serialises to text, byte-identical since v28) — reads records, stores nothing; see [docs/design-docs/calendar-export.md](docs/design-docs/calendar-export.md) | `const buildIcs =` |
| State lifecycle | `migrate` (v11 → v28 blocks, `@schema` JSDoc above it), `applyDailyTick`, `freshState`, `demoState` | `const migrate =` |
| Onboarding | `OptRow` (shared chip row) and the shared CV entry components `CvEntryRow` / `CvAddForm` / `CvSection` (education and career add-and-list forms, reused by `ProfileModal`), then `Onboarding` (6 steps: basics + education record → appearance → areas & directions → qualifications/exams → career record + experience → computed grades) | `function Onboarding` |
| Tabs | `HomeTab` (rewritten 2026-09-15 into one CV card plus a role-model row, nav-renamed `home`/`프로필` and its `자격`/`시험` rows stopped printing `D{n}` 2026-09-16; gained a second stage line under the proximity line, v28, replaced 2026-09-18 by a stage-progress headline with a bar and a verdict-probability caption above the proximity line, both retired the same day, third role-model change of the day ([Rule 14](docs/design-docs/core-beliefs.md#rule-14) amendment) — the row is now one line, `단계 k/n {stageName(rs)} · 조건 c/m ›` with stages, `{role.name} · 단계 없음 …` with a role and none, or the inert `롤모델 미설정 …` without a role, no `%` anywhere; module-level `CvFact` (gained an optional `wrap` prop 2026-09-16) and `AreaGradeRow` rows, helpers `cvSummaryOf`/`heldCertsOf`/`examBestText` above), `GoalsTab`, `TaskTab` (rewritten 2026-09-13 into one time-ordered list over `todoOf`; rewritten again 2026-09-16 into compact `TodoRow`s — the shared row shell `TodoRow`, lead-chip helper `todoLeadOf`, tone map `TODO_TONE`, archive cap `TODO_DONE_MAX`; `BizTodoRow` and `DueChip` deleted, `EventRow`'s `tail` prop deleted; three sibling detail-sheet modals `TaskDetailModal`/`EventDetailModal`/`BizTodoModal` hold what the old row printed, completion moved entirely into `TaskDetailModal`'s `완료하기`; nav-renamed `tasks`/`할 일`; gained the `브리핑 열기 ›` header button 2026-09-15) — the fourth tab, `WorkTab` (new 2026-09-17, own region below), the fifth, `ScheduleTab` (calendar-only since 2026-09-16, its `목록` view and `setScheduleView` deleted), the sixth, `MeetingsTab` (new 2026-09-16, own region below), and the seventh, `BizTab`, each have their own region below. The former sixth tab, `GrowthTab`, was deleted 2026-09-15: its role-model headline, skill track, wall and data panel became the home CV, the `롤모델` screen (`RoleModal`, 2026-09-18, replacing that rewrite's `RoleAdviceModal`) and two modals from that rewrite (below) | `function GoalsTab` |
| Schedule | `EventRow` (the occurrence row the calendar's selected-day panel **and** `할 일`'s `EventDetailModal` both render; its `tail` prop deleted 2026-09-16), `ScheduleCalendar` (month grid, markers, day-number tones, selected-day panel, `CAL_RANGE_MONTHS`, `WEEKDAY_LABEL`, `HOLIDAYS`, `HOLIDAY_YEARS`), `ScheduleTab` (calendar-only since 2026-09-16 — the `목록`/`달력` toggle, its day groups and the header's own `일정 추가` button are all deleted; only the counts header and `ScheduleCalendar` remain), `EventModal` (gained a `projects` prop and an optional `projectId` field, schema v26 — [docs/product-specs/meetings.md](docs/product-specs/meetings.md)) — appointments and deadlines, never tasks — `EventChecks` (v27, the `확인할 것` checklist shared by `MeetingPrepCard` and `EventDetailModal`, which gained the same three check handlers for a project-linked event's checklist) — plus `CalendarExportModal`, which reads `calendarExportOf` for its preview and writes a file, never state | `function ScheduleTab` |
| Meetings (new 2026-09-16, schema v23; task links v24; progress log + `aiHidden` v25; follow-up items + prep rows v26; urgent memos + transcript 2026-09-17, still v26; documents + pre-meeting checks v27; tracks v28; `kind: "training"` records 2026-09-22, still v28, made reference only the same day) | `MEETING_KIND`, `MEETING_KIND_OPTIONS`, `isTraining`, `followUpsOf` (2026-09-22, same-day: `[]` for a training record, the one helper every to-do surface/packet/calendar entry reads instead of `m.followUps`), `MEETING_FIELD_LABEL`, `meetingLabels`, `kindPrefix`, `lastMeetingOf` (a project's last meeting-kind record, a training record standing in only when none exists; `docs/product-specs/meetings.md#training-records-교육-2026-09-22--reference-only-same-day`), `meetingTrack(state, m)` (v28, a project meeting inherits `trackOf` of its project, a memo reads its own field), `TrackRow` (shared with every other form; a memo's own field written only while `pid === null`), `MeetingsTab` (project sections of compact `TodoRow`-shaped minutes rows, `{n}건 더 보기` expansion, `meetingRowMarker` — `진행 {n}건` and `후속 {open}/{total}` since v25/v26 — plus, since 2026-09-17, an always-rendered project-less group `프로젝트 없음 · 긴급 메모` (`memos`, `rowsBlock` shared helper), collecting every `projectId == null` meeting; gained, v27, a `문서 {n}건` documents block per project and per the memo group (`docsBlock`, `docRow`, `memoDocs`), `문서 추가` buttons, and `rowsBlock`'s now-generalised fourth `rowOf` argument), `ProjectModal` (gained `documentCount` v27, disabling `삭제` on either count), `ProjectPicker` (v27, the `프로젝트` chip row extracted from `MeetingModal` and shared with `DocumentModal`), `DocumentModal` (v27, add/view/edit/confirm-delete for one document), `MeetingModal` (score field-free; caps `MEETING_LIMITS`/`PROJECT_LIMITS`, textarea counter `MeetingText`; gained the `AI에 보내지 않기` checkbox 2026-09-17 and the follow-up item editor v26; gained, also 2026-09-17, a `없음 (긴급 메모)` project chip and a collapsed `녹취록` transcript block behind `녹취록 붙여넣기`; gained, v27, `확인할 것 가져오기` copying a linked event's checks into follow-up rows; 2026-09-22, same-day: hides the task-link picker, the follow-up editor and `확인할 것 가져오기` entirely for a training record, and writes `taskIds`/`followUps` back untouched on one), `MeetingViewModal` (gained the `진행사항` add/list/delete block and the `AI 전송` fact row 2026-09-17, the follow-up checklist and `항목으로 나누기` split panel v26; gained, also 2026-09-17, a `녹취록 {n}자 · 펼치기`/`접기` block and `녹취록 지우기`, plus the `없음 (긴급 메모)` project fact; 2026-09-22, same-day: hides the `후속 항목`, `진행사항` and `연결된 할 일` blocks entirely for a training record), helpers `meetingOrder`, `meetingEventText`, `mbText`, `taskClosedOn`, `meetingTaskCandidates`, `meetingsOfTask`, `linkedTaskLead`, `splitFollowUpText`, `followUpWorkDate`, `followUpWorkItem`, `reconcileFollowUps`, `meetingPrepOf` (gained `docs` v27), `docOrder` (v27), `eventProjectOf` (v27, extracted so `meetingPrepOf` and `buildPrepPacket` share the match), `dealsOfProject` (v27), `checkTexts` (v27), constants `MEETING_ROWS_SHOWN`, `MEETING_TASK_PAST_DAYS`, `MEETING_TASK_ROWS`, `MEETING_PROGRESS_MAX`, `MEETING_FOLLOWUPS_MAX`, `MEETING_LIMITS.transcript` (30000, 2026-09-17), `PREP_DAYS`, `PREP_FOLLOWUPS`, `PREP_PROGRESS`, `PREP_TASKS`, `PREP_DECISION_CLIP`, `DOC_LIMITS`, `DOC_SUMMARY_CLIP`, `PREP_DOCS` (v27), `EVENT_CHECKS_MAX`, `EVENT_CHECK_TEXT` (v27) — `meetings[].taskIds` links up to 10 existing tasks, listed on the meeting view and, derived, on the task sheet's `관련 회의록`; `meetings[].followUps` (v26) are structured follow-up items, a `mine` one mirrored to a `work[]` item both ways on `done`; `meetings[].projectId` may be `null` and `meetings[].transcript` (optional, ≤ 30,000 chars, kept as pasted, never read by any packet) since 2026-09-17, no schema bump; top-level `documents[]` (v27) — a record on a project or none, never a file (`docs/product-specs/documents.md`); hand-written minutes grouped by project, a record never a task; see [docs/product-specs/meetings.md](docs/product-specs/meetings.md) | `function MeetingsTab` |
| Daily work (new 2026-09-17, schema v25; derived carry-forward, the meeting-prep card and follow-up mirroring v26; `회의록 열기` 2026-09-17, still v26; documents + checklist + `AI에게 회의 준비 묻기` v27; tracks + time log + payment lines v28; `act.workRefreshedAt` + the since-mode work packet 2026-09-22, still v28) | `TIME_LOG_MAX_MINUTES`, `weekMinutes`/`weekDaysLeft`/`hoursText`/`bizHoursOf`/`timeLine` (shared with the reader and `TimeLogModal`), `syncTimeLog` (root, keeps exactly one `timeLog` entry per done-with-minutes item), `TimeLogModal` (v28, `사업 시간 기록`), `MeetingPrepCard` (`오늘 회의 준비`, first section of the tab, v26; a `div` since v27 — TD-64, an explicit `회의록 열기 ›` button replaces the old whole-block tap because the v27 checklist's controls cannot nest inside a button — plus the project's documents and `<EventChecks>` and an `AI에게 회의 준비 묻기` button, five new props), `WorkTab` (day pager; `지난 미완료` and its `오늘로 옮기기` button deleted v26, replaced by derived carry-forward on today's own view; gained four props v27, passed to `MeetingPrepCard`), `WorkModal` (add/edit/complete/delete in one sheet; gained `onOpenMeeting` and a `회의록 열기` button 2026-09-17 for a meeting-linked item with a live link — replaces the sheet with `MeetingViewModal` in the single modal slot, [TD-58](docs/exec-plans/tech-debt-tracker.md)), `WorkBridgeModal` (`오늘 업무 만들기` — shares `PacketSendPane`/`ReplyPastePane`/`copyPacket` with `BridgeModal`; caption gained one sentence 2026-09-17: `녹취록은 실리지 않아요.`; each open confirm row gains a `BizChips` track-pick row, 2026-09-22, preselected by `importTrack` or `proposalTrackOf`; gains an optional `since` prop and a `지난 갱신 이후`/`전체` chip pair, 2026-09-22, `docs/product-specs/daily-work.md#the-incremental-packet--actworkrefreshedat-and-since-mode-2026-09-22`), `PrepBridgeModal` (v27, the third bridge packet's confirm modal, own region below), helpers `workOn` (signature widened to `(state, date, today)` v26, prefixes today's view with carried items), `workLeadOf` (replaces the deleted `workPastOpen`/inline `sourceLead`; states `이월 {n}일` or the source chip), `workLinkLabel`, `workLinkText`, `workLinkOptions`, `normWorkTitle`, constants `WORK_LIMITS`, `WORK_LINK_MEETINGS`, `WORK_KIND_WORD` — `work[]` is a dated record outside the goal ladder, never a task, with a third `source: "meeting"` value (v26) for an item a follow-up mirrors; see [docs/product-specs/daily-work.md](docs/product-specs/daily-work.md) | `function WorkTab` |
| Business tab | `BizTab` (`계약` / `단가` / `포트폴리오` / `로드맵` / `리드` / `공고` views since v28, `BIZ_ADD_LABEL`/`BIZ_ADD_MODAL`), `DealsView`, `RatesView`, `FolioView` (its own thumbnail-loading effect), row components (`BizRowHead`, `MoneyLine`, `DealRow` — gains a track tag and payment chips v28), `DealModal` (gains a track row, a payment-line block and a `prefill` prop v28), `RateModal`, `FolioModal`, `RoadmapView`/`MilestoneModal`/`MilestoneLinkList` (v28), `LeadsView`/`LeadModal`/`FormLabel` (v28), `NoticesView`/`NoticeModal`/`NOTICE_TONE` (v28) — contracts, unit prices, the portfolio, the roadmap, the pipeline and notices, never tasks | `function BizTab` |
| Modals | `AddGoalModal`, `EvidenceViewModal`, `CatalogModal` (gained an optional `initialQuery` prop 2026-09-18, seeding its search input for the `롤모델` screen's `cert_held` landing), `AddTaskModal`, `EvidenceModal` (gained a `점수` field for exam tasks 2026-09-16), `ActivityLogModal`, `StudyVerifyModal`, `PromoteModal`, `RoleModal` (2026-09-18, the `롤모델` screen, `modal.type: "role"` — replaces `RoleModelModal`'s and `RoleAdviceModal`'s user-facing roles; own region), `RoleGradeSection` (2026-09-18, module-level, `RoleAdviceModal`'s body moved verbatim, rendered by `RoleModal`'s collapsed `영역 등급` section only; its bars and legend paragraph retired the same day, third role-model change of the day), `RoleStageLines` (takes `rs` from `roleStageOf` since the same change, no longer `sp`; dropped its `진행 · 전체` line and per-condition bars), `RoleModelModal` (now the `roleEdit` sub-screen, title `롤모델 세부 수정`, reached through `RoleModal`'s `세부 수정 ›`; lost its preset chip row and story field, gained `기본 9단계 채우기`; `RoleAdviceModal` itself deleted), `ProfileModal` (the CV screen — photo, personal facts, education/career records, read-only held records), `SettingsModal` (role model, weekly business-hours budget, the day-job-in-AI-packets switch (2026-09-22, `AI 요청문` section, on by default), the `확인 필요` notification switch (2026-09-22, `확인 알림` section, `docs/product-specs/notifications.md`), backup, reset — behind home's corner `설정` button, 2026-09-15), `IssueListModal` (2026-09-22, `이슈 목록`, `modal.type: "issues"`, `docs/product-specs/issue-list.md`, Modals region next to `DailyReaderModal`), `AchievementWallModal` (trophies, specialisations, exam bests, per-area achievements, exam score since 2026-09-16 — behind the CV's `성취` row, 2026-09-15), `TaskDetailModal`, `EventDetailModal`, `BizTodoModal` (the `할 일` row detail sheets, new 2026-09-16), `ProjectModal`, `MeetingModal`, `MeetingViewModal`, `DocumentModal` (v27, the `미팅` sheets, above), `WorkModal`, `WorkBridgeModal` (generalised v28 to take `build`/`title`/`caption`/`importDate`/`importTrack` props, rendered a second time as `reviewBridge`; each confirm row's track pick, 2026-09-22, above), `PrepBridgeModal` (v27, the `업무` sheets, above), `DailyReaderModal` (v27, `오늘 읽을 것`, own region below `BriefingModal`; gained `roadmap`/`pipeline` sections and track heads v28, a tenth section `role` 2026-09-18), `RoleVerdictModal` (2026-09-18, the fifth bridge packet's send/paste/confirm sheet, `modal.type: "roleVerdict"`, opened from the `롤모델` screen's `AI에게 판정 묻기 ›` button — `RoleAdviceModal`'s until the same-day role-screen rewrite — reusing `PacketSendPane`/`ReplyPastePane`/`copyPacket`) | `function AddTaskModal` |
| Feedback | `ToastHost` (ref API), `Overlay` (`gradeup` / `achieve` / `stage` — 2026-09-18, no portrait, no trophy, raised once per stage by the root effect below) | `const ToastHost =` |
| App root | `LifeManager`: load → migrate → render; handlers `completeTask` (gained an optional third `score` argument 2026-09-16), `tryComplete`, `promoteArea`, `addGoal`, `goalStatus`, `checkinKR`, `setAreaDir`, `applyMeasures`, `removeTask`, `removeGoal`, `resetAll`, `saveProfile`, `addBiz`, `updateBiz`, `removeBiz`, `toggleDealPaid`, `addProject`, `updateProject`, `removeProject` (v27: also refuses a project with documents, after the minutes guard), `commitMeeting` (new v26 — reconciles a meeting write against `reconcileFollowUps`, checks the budget for the meeting plus every mirrored work item, and is what `addMeeting`/`updateMeeting` now call; 2026-09-22, same-day: skips `reconcileFollowUps` for a training record, so saving one never creates, changes or removes a work item), `addMeeting`, `updateMeeting`, `removeMeeting`, `putMeeting` (still used by `addProgress`/`removeProgress`), `toggleFollowUp`, `setFollowUpMine`, `appendFollowUps` (new v26, [docs/product-specs/meetings.md](docs/product-specs/meetings.md)), `recordFits` (renamed from `meetingFits` 2026-09-17, now shared by meetings, progress entries, work items, documents and event checks, `docs/product-specs/meetings.md`), `addProgress`, `removeProgress`, `clearTranscript` (new 2026-09-17 — removes only a meeting's `transcript` key via `putMeeting`, no budget check, `docs/product-specs/meetings.md`), `addWork`, `updateWork`, `toggleWork` (mirrors a linked follow-up's `done` v26), `removeWork`/`removeWorkMany` (unlink a mirrored follow-up's `workId` v26, via the shared `dropWork`), `importWork` (new 2026-09-17, `docs/product-specs/daily-work.md`), `addDocument`, `updateDocument`, `removeDocument` (new v27, `docs/product-specs/documents.md`), `writeChecks`, `addCheck`, `toggleCheck`, `removeCheck`, `importChecks` (new v27, one event's `checks` only, `docs/product-specs/schedule.md`), `downloadBlob` (the one shared create-URL/anchor/revoke download path), `exportCalendar` (builds with `buildIcs`, downloads, toasts — reads state, writes nothing); (v28) `toggleDealPayment`, `addMilestone`, `updateMilestone`, `removeMilestone`, `seedRoadmap`, `addLead`, `updateLead`, `removeLead`, `makeDealFromLead`, `addNotice`, `updateNotice`, `removeNotice` (`docs/product-specs/business.md`), `addTimeLog`, `removeTimeLog`, `setBizHours` (`docs/product-specs/daily-work.md`, `docs/product-specs/home.md`), `addBiz`'s new optional `leadId` argument, `importWork`'s new optional `date` argument (its `track` argument and the old `importTrackOf` helper are gone, 2026-09-22 — the picked `track` travels on each proposal instead); (2026-09-22) `setWorkInAi` (writes `settings.workInAi` only, an explicit `true`/`false`, `docs/product-specs/home.md`); (2026-09-22, the `확인 필요` notification) `checkRegistration`, `checkCacheRead`, `checkCacheWrite`, `checkNotificationsClose`, `checkSyncRegister`, `checkNotifyTeardown`, `checkRefresh` (module-level helpers above the root, all guarded, `docs/product-specs/notifications.md`), the root's `setCheckNotify` (writes `settings.checkNotify` only), the debounced refresh `useEffect` on `[state, today]`, the boot effect's `?open=` param read (`OPEN_PARAM_TYPES`, `history.replaceState`) and its once-at-boot `checkSyncRegister()`, and a `navigator.serviceWorker` `message`-listener effect (`{ open: "issues" | "reader" }`); (2026-09-18) `saveRole` (extracted from the inline `role` `onSave`; writes `role` only, stamps `seenStageK` to the newly-derived `k` so a stage set the user just wrote never fires the completion overlay), `importRoleVerdict` (writes `role` only from the verdict sheet's ticked stages/areas/verdict, `importChecks`-shaped refusal return, never calls `upsertReply`), the stage-completion overlay effect (after the day-change effect — compares `roleStageOf(state, today).k` against the `role.seenStageK` seen-stamp, raises `{ type: "stage" }` and re-stamps in the same effect; the single place that overlay is raised); (2026-09-18, second change of the day) `saveRoleStory` (writes `role.story` alone, never `stages`/`targets`/`verdicts`/`seenStageK`), `resetRole` (deletes the whole `role` record on a `window.confirm`, closes every modal — the one handler in this change flagged under AGENTS.md §4 for deleting user data), `goRoleAction` (the one landing per `ROLE_COND_ACTIONS[type]` — a business view/tab plus its add modal, or the catalogue — writing only `ui.bizView` through the existing `setBizView`); `saveRole`'s write guard changed to `if ("story" in rm) { … }` so the `roleEdit` sub-screen's save leaves `role.story` untouched; `Shell`, `NAV` (`grid-cols-7` since 2026-09-17); the boot and day-change effects open `{ type: "reader" }` in the briefing's former place (v27, [docs/product-specs/daily-reader.md](docs/product-specs/daily-reader.md)); `closeBriefing` unchanged in code, now also the reader's close/route path; `setScheduleView` deleted 2026-09-16, `moveWorkToToday` deleted 2026-09-17 (v26, carry-forward is derived) | `export default function LifeManager` |

The data regions (`CERTS`, `EXAMS`, matrices, option lists) are about a quarter of the file; the harness skips them for duplicate detection and the `data-guard` hook protects them. Splitting the file into `src/data`, `src/engine`, `src/components` is backlog item 3 (`docs/exec-plans/backlog.md`), to be done with unit tests for `calcExamPayout`, `krProgress`, `migrate`.

## State flow
1. Mount: `store.get(KEY)` → `migrate` → `applyDailyTick` (monthly shield reset) → `setState`; missing state → onboarding.
2. Every `setState` persists the whole object (`useEffect` on `state`). Images are separate keys.
3. Handlers mutate a `structuredClone` of the previous state and return it; effects (toasts, overlays) are queued with `queueMicrotask`.
4. Derived numbers (progress, pace, role-model requirement-gap facts, job fit, payouts) are recomputed in render from state + frozen tables.

Schema and storage keys: [docs/generated/db-schema.md](docs/generated/db-schema.md). Engine formulas: [job-weighting.md](docs/design-docs/job-weighting.md) (scoring-engine.md follows in Phase 4 — see [docs/design-docs/index.md](docs/design-docs/index.md)). Screens: [docs/product-specs/index.md](docs/product-specs/index.md).

## Builds and commands
| Command | Effect |
|---|---|
| `npm run dev` / `build` / `preview` | Vite dev server / production build to `dist/` / static preview |
| `npm run build:demo` | single-file demo `release/life-demo.html` (JS + CSS inlined, favicon as data URI; `release/` is git-ignored). iOS cannot run JS from `file://` — host it for phones |
| `npm run verify` | build → preview on a free port → E2E → report (add `-- --smoke` for engine checks) |
| `npm run finish` | dead code / duplicates / residue / language gate (also the Stop hook) |
| `npm run smoke` | pure-function engine checks |
| `npm run docs:gen` / `docs:check` / `lang:check` | regenerate generated docs / doc integrity / language policy |
| `node tools/harness/gen-icons.js` | re-render the app icons from `public/icon.svg` (after an icon change) |

Current status (2026-09-22): schema **v28** (v22 exam scores, v23 meeting records, v24 meeting task links, v25 daily work items and meeting progress, v26 meeting follow-up items, v27 documents (`documents[]`) with an optional `events[].checks`, v28 tracks/roadmap/time budget/payment lines/role stages/pipeline/notices, plus three optional `role` fields added 2026-09-18 — story/verdicts/seenStageK — and three more optional fields added 2026-09-22 — `act.workRefreshedAt`, `settings.checkNotify`, `meetings[].kind` — all with no `v` bump — [docs/design-docs/state-lifecycle.md](docs/design-docs/state-lifecycle.md)), difficulty table **V1.3** (`DIFF_RAW_VERSION "1.3"`, 1,011 certifications, 17 exam families, 21 jobs × 15 categories), **seven tabs** (`프로필 · 목표 · 할 일 · 업무 · 일정 · 미팅 · 사업`, `성장` retired 2026-09-15, `미팅` added 2026-09-16, `업무` added 2026-09-17 — home is one CV plus one row opening the `롤모델` screen (`RoleModal`, replacing `RoleAdviceModal` 2026-09-18): a stage line (`단계 k/n {name} · 조건 c/m`) with stages, a `단계 없음` line with a role and none, or an inert `롤모델 미설정` line without a role — no percentage anywhere on the role model since 2026-09-18's third role-model change of the day (the [Rule 14](docs/design-docs/core-beliefs.md#rule-14) amendment retired the stage-progress headline's bar/caption, the proximity line and its segmented bars, the verdict probability, and the RANK UP overlay's proximity delta, all the same day), `modal.type` 22 → 28 → 30 → 33 → 38 → 39 → **40** (v28: `milestone`, `timeLog`, `lead`, `notice`, `reviewBridge`; 2026-09-18 first change: `roleVerdict`; 2026-09-18 second change: `roleAdvice` retired, `roleEdit` added, count stayed 39; 2026-09-22: `issues` added, `IssueListModal`, the `이슈 목록` overview), three `Overlay` branches since 2026-09-18 (`gradeup`, `achieve`, `stage`)), E2E steps **updated, not run**: `tools/e2e/flow*.js` was edited in every phase of the check-notification/incremental-packet/issue-list plan (2026-09-22) on top of the work-proposal-track-pick and day-job-AI-switch changes (also 2026-09-22) and the role-model-no-percent change (2026-09-18, third role-model change of the day) before them, and counts **268** `await step(` calls as written (`flow.js` 38, `flow2.js` 16, `flow3.js` 25, `flow4.js` 23, `flow5.js` 19, `flow6.js` 9, `flow7.js` 27, `flow8.js` 28, `flow9.js` 10, `flow10.js` 32, `flow11.js` 41), but the standing user instruction for this change means the suite was not executed after the edits — see [docs/RELIABILITY.md](docs/RELIABILITY.md). Real-device smoke still pending (backlog 2, now including the calendar-file import and alarm check, and a real Periodic Background Sync firing per [notifications.md](docs/product-specs/notifications.md)).

## Platform notes
- The app was originally an artifact that used `window.storage`; it was shimmed to `localStorage` on 2026-09-03 behind the same `store` interface — that interface is the seam if a different backend is ever needed.
- `askClaude` and all AI features were removed on 2026-08-29; nothing in the code calls a model.

## Glossary
| Korean (UI/data) | English (docs/code) |
|---|---|
| 실행 | task (`tasks`, `TaskTab`, `AddTaskModal`) — task created under a goal; the `할 일` tab (below) shows tasks alongside events and business rows |
| 할 일 (tab) | to-do — the time-ordered list of tasks, events and business rows on the `tasks` tab (`TaskTab`, nav label `할 일` since 2026-09-16, renamed from `실행`); not a data type of its own |
| 영역 | area (`areas`, `areaId`, `AREA_PRESETS`) |
| 목표 / 핵심결과 (KR) | goal / key result (`goals`, `krs`, types `metric` `count` `exam` `cert`) |
| 마일스톤 | milestone — a certification, exam, or study task exempt from the one-day size rule |
| 등급 (0–9) | area grade (`RANKS`) |
| 성취 · 성취의 벽 | achievement · achievement wall (`room.trophies`, kinds `ach` `rank` `spec`) |
| 관문 · 승급 | gate · promotion (`GATE_CHIPS`, `PromoteModal`, `promoteArea`) |
| 롤모델 요구 영역 | role-model requirement facts — per-area required grade, verified grade and gap (`roleAreas`, renamed from `roleGap` 2026-09-18, third role-model change of the day, no `match`/percentage since the [Rule 14](docs/design-docs/core-beliefs.md#rule-14) amendment) |
| 스트릭 · 보호권 | streak · streak shield (`act.streak`, `act.shieldsLeft`) |
| 직무 적합 (S/A/B/C) | job fit tier (`WEIGHT_MATRIX`, `TIER_MULT`) |
| 증거 | evidence (`task.evidence`, `liferpg-img-ev-*`) |
| 도감 | catalog (`CatalogModal`) |
| 일정 (약속 · 마감) | schedule event — appointment / deadline (`events`, `ScheduleTab`, `EventModal`); a record, never a task |
| 미팅 (프로젝트 · 회의록 · 긴급 메모) | meeting minutes — project (`meetingProjects`) and its hand-written minutes (`meetings`, `MeetingsTab`, `ProjectModal`/`MeetingModal`/`MeetingViewModal`, schema v23; `taskIds` links existing tasks, schema v24; `progress[]`/`aiHidden` schema v25; structured follow-up items `followUps[]`, mirrored to a work item, schema v26; project-less urgent memos (`projectId: null`) and an optional pasted `transcript` (≤ 30,000 chars, never in a packet) since 2026-09-17, no schema change; an optional `kind: "training"` since 2026-09-22, no schema change); a record, never a task, no time of its own — see 일정 above |
| 교육 (회의록 종류) | training record — a meeting record (`meetings[].kind === "training"`) reusing every field of a meeting under different labels (`강사·주최`/`배운 것`/`핵심 정리`/`적용할 것`, `MEETING_FIELD_LABEL`, 2026-09-22); never a project's `lastMeetingOf` while a meeting-kind record exists for the same project — see [docs/product-specs/meetings.md](docs/product-specs/meetings.md#training-records-교육-2026-09-22) |
| 이슈 목록 | issue list — a persistent overview (`issueListOf`, `IssueListModal`, `modal.type: "issues"`, 2026-09-22): open work/tasks/follow-ups by track, the next 14 days of schedule, the newest training records, each project's latest minutes; every row opens an existing sheet, nothing here writes state — see [docs/product-specs/issue-list.md](docs/product-specs/issue-list.md) |
| 확인 필요 (알림) | the local `확인 필요` notification (`settings.checkNotify`, absent = off, `checkSummaryOf`/`checkNotificationOf`, 2026-09-22) — three facts mirrored into a Cache API entry (`life-check`) the service worker re-shows; no network, no push server — see [docs/product-specs/notifications.md](docs/product-specs/notifications.md) |
| 업무 | daily work item — a dated record outside the goal ladder (`work`, `WorkTab`, `WorkModal`, `WorkBridgeModal`, schema v25; a third `source: "meeting"`, derived carry-forward and the `MeetingPrepCard` schema v26); typed by hand, proposed by the assistant bridge and confirmed per item, or mirrored from a meeting follow-up; a record, never a task |
| 문서 | document — a top-level record of what a file says (`documents`, `DocumentModal`, schema v27), on a meeting project or none; no file is ever stored, only the summary text and an optional file name/link (`source`) as text; a record, never a task |
| 확인할 것 | pre-meeting check — a checklist item on a schedule event (`events[].checks`, `EventChecks`, schema v27), edited on the meeting-prep card and the event sheet, or imported from the third bridge packet's reply; a record, never a task |
| 오늘 읽을 것 | daily reader — a full-content modal derived from the saved state (`buildReader`, `DailyReaderModal`, schema v27) that opens once a day in the briefing's former place; stores nothing beyond the pre-existing `act.briefingSeen` stamp |
| 사업 (계약 · 단가 · 포트폴리오) | business — contract (deal) · rate · portfolio piece (`deals`, `rates`, `folio`, `BizTab`); a record, never a task |
| 트랙 (직장 · 사업 · 개인) | track — the day job, the business, or private life (`track`, `TRACKS`, `TrackRow`, schema v28) stored on a project, document, event, work item and deal, and derived for a meeting (`meetingTrack`); whether a `work`-track record enters an AI packet is the `settings.workInAi` switch (2026-09-22, on by default, `설정 › AI 요청문`) |
| 마일스톤 (로드맵) | roadmap milestone — a business record with a user-set status, D-day, linked-work completion and pace (`milestones`, `MilestoneModal`, `RoadmapView`, schema v28) — **distinct from** the goal-ladder 마일스톤 of Rule 18 (a certification/exam/study task exempt from the one-day size rule); the two share a Korean word but never a data shape |
| 리드 | lead — a hospital sales-pipeline record (`leads`, `LeadModal`, `LeadsView`, schema v28); converts to a contract only through a prefilled `DealModal`, never by itself |
| 공고 | notice — a national-project call record (`notices`, `NoticeModal`, `NoticesView`, schema v28); kept out of the AI packets, carried in the calendar file's deadline entry |
| 시간 기록 | time log — a dated, tracked minutes entry (`timeLog`, `TimeLogModal`, schema v28); the single source `weekMinutes` sums, never `work[].minutes` directly |
| 단계 (롤모델) | role stage — one of up to twelve editable stages with fact conditions (`role.stages`, `roleStageOf`, the stages editor inside the `roleEdit` sub-screen — `RoleModelModal`, reached through the `롤모델` screen's `세부 수정 ›` since 2026-09-18 — schema v28); stated as `단계 k/n · 조건 c/m` counts only — `stageProgressOf`'s progress/journey percentages existed for a few hours on 2026-09-18 and were retired the same day (third role-model change of the day, [Rule 14](docs/design-docs/core-beliefs.md#rule-14) amendment) |
| 입금 예정 (일시금) | payment line — a lump-sum deposit/interim/final/other line on a contract (`deals[].payments`, schema v28), beside the monthly `paidMonths` stamps; never summed into the monthly totals, never carries its amount into the calendar file |
| 판정 (롤모델) | role verdict — an AI-stated, unverified reply to the fifth bridge packet (`role.verdicts`, `RoleVerdictModal`, `importRoleVerdict`, 2026-09-18); dated, capped at 24, shown under the label `AI 판단 · 검증되지 않음`; never turned into a grade, payout or promotion; carries no probability since 2026-09-18 (third role-model change of the day, [Rule 14](docs/design-docs/core-beliefs.md#rule-14) amendment) — an older verdict's `probability` field, if any, is left in the save unread |
